# 渠道「参数覆盖」与「请求头覆盖」辨析

> 更新于 2026-07-15  
> 依据：`relay/common/override.go`、`relay/channel/api_request.go`、渠道编辑 UI（`param_override` / `header_override`）  
> 目的：把容易配错的点一次说清（尤其是 User-Agent）

渠道高级设置里有两块配置，**不是**「两块都改请求头、只是一块更高级」：

| UI 文案 | 存库字段 | 主战场 | 也能改请求头？ |
|---|---|---|---|
| **Parameter Override / 参数覆盖** | `channels.param_override` | **请求 JSON Body** | 能，但要用 `set_header` / `pass_headers` 等 |
| **Request Header Override / 请求头覆盖** | `channels.header_override` | **HTTP 请求头** | 专门干这个 |

最终上游请求头由 `processHeaderOverride` → `GetEffectiveHeaderOverride` 决定：

- 未走参数覆盖的 header 操作时：用渠道 **请求头覆盖**
- 参数覆盖执行了 `set_header` / `pass_headers` 等后：写入 `RuntimeHeadersOverride`，并优先用它

---

## 0. 一句话怎么选

| 需求 | 填哪里 | 写法要点 |
|---|---|---|
| 固定 `User-Agent`（有则覆盖、无则补上） | **请求头覆盖** | `"User-Agent": "固定字符串"` |
| 固定 Authorization 为渠道 key | **请求头覆盖** | `"Authorization": "Bearer {api_key}"` |
| 透传客户端大部分头 + 再强制几个头 | **请求头覆盖** | `"*": true` + 显式键 |
| 改 `temperature` / 嵌套 body 字段 | **参数覆盖** | `mode: "set"` + `path` 指向 **body 字段** |
| 仅某些模型才改 body/头 | **参数覆盖** | 加 `conditions` |
| 用参数覆盖改 HTTP 头 | **参数覆盖** | 必须 `mode: "set_header"`，**不要** `set` |

---

## 1. 最易错点：`set` 的 `User-Agent` 不是改请求头

### 错误示例（看起来像在改 UA，实际改 body）

```json
{
  "operations": [
    {
      "mode": "set",
      "path": "User-Agent",
      "value": "codex-tui/0.144.3 (Mac OS 15.6.1; arm64) ghostty/1.3.1 (codex-tui; 0.144.3)"
    }
  ]
}
```

| 项 | 实际效果 |
|---|---|
| `mode: "set"` | 只动 **JSON Body**（`sjson.SetBytes`） |
| `path: "User-Agent"` | body 里多一个 / 改一个 `"User-Agent"` 字段 |
| 上游 HTTP `User-Agent` | **不变** |
| 常见 chat/completions body | 本来就没有该字段 → 多塞一个上游常忽略的字段 → **像没配** |

### 正确：改 HTTP 头

**方式 A（推荐，固定值）——请求头覆盖：**

```json
{
  "User-Agent": "codex-tui/0.144.3 (Mac OS 15.6.1; arm64) ghostty/1.3.1 (codex-tui; 0.144.3)"
}
```

**方式 B——参数覆盖里用 header mode：**

```json
{
  "operations": [
    {
      "mode": "set_header",
      "path": "User-Agent",
      "value": "codex-tui/0.144.3 (Mac OS 15.6.1; arm64) ghostty/1.3.1 (codex-tui; 0.144.3)"
    }
  ]
}
```

---

## 2. `set` 的三种语义（body）

实现：`applyOperations` 的 `case "set"`（`relay/common/override.go`）。

| 配置 | 字段已存在 | 字段不存在 |
|---|---|---|
| `mode: "set"`（默认） | **覆盖** | **新建** |
| `mode: "set"` + `keep_origin: true` | **跳过（保留原值）** | **新建** |
| 想要「有才覆盖、没有不加」 | **覆盖** | **不添加** → **没有单独 mode**，要用 `conditions` 做存在性门禁 |

`keep_origin` UI 文案含义是：**目标已存在就保留**，和「有则改、无则不加」正好相反，别混。

### 「有才覆盖、没有不加」写法（body 示例）

```json
{
  "operations": [
    {
      "mode": "set",
      "path": "temperature",
      "value": 0.7,
      "conditions": [
        {
          "path": "temperature",
          "mode": "prefix",
          "value": "",
          "pass_missing_key": false
        }
      ]
    }
  ]
}
```

说明：路径缺失时条件默认失败（`pass_missing_key: false`），整条 `set` 被跳过，因此不会无中生有。

---

## 3. 请求头覆盖（`header_override`）

字段：渠道编辑 → **Request Header Override**。

### 3.1 规则类型

| 类型 | 语法 | 含义 |
|---|---|---|
| 全透传 | `"*": true` | 尽量透传客户端 header（值可写 `true`，只认 key） |
| 正则透传 | `"re:^X-Trace-.*$": true` / `"regex:..."` | 名称匹配 Go regexp 的客户端头透传 |
| 强制覆盖 | `"User-Agent": "固定值"` | **有则覆盖、无则补上** |

名称匹配时会规范化为小写。

### 3.2 处理顺序

**先透传（`*` / `re:`）→ 再写显式键。**  
因此显式的 `User-Agent` / `Authorization` 会压过透传同名头。

### 3.3 占位符

| 占位符 | 说明 |
|---|---|
| `{api_key}` | 替换为当前渠道 API Key |
| `{client_header:<名>}` | **整段 value 必须是该占位符**；客户端没带该头则**跳过不设置**（没有 `\|fallback` 语法） |

要「客户端没带也给默认值」：不要用 `{client_header:...}`，直接写死强制覆盖字符串。

### 3.4 推荐实配（Codex 固定 UA）

你当前可用配置：

```json
{
  "*": true,
  "Authorization": "Bearer {api_key}",
  "User-Agent": "codex-tui/0.144.3 (Mac OS 15.6.1; arm64) ghostty/1.3.1 (codex-tui; 0.144.3)"
}
```

| 配置项 | 作用 |
|---|---|
| `"*": true` | 先透传客户端安全 header |
| `"Authorization": "Bearer {api_key}"` | 用渠道密钥覆盖认证头 |
| `"User-Agent": "..."` | 固定上游 UA（有无客户端 UA 都落到该值） |

注意：

1. `*` 会透传较多客户端头；若只想固定少数头，可去掉 `*`，只留显式键。  
2. 同一需求**不必**再在参数覆盖里写 `set` + `User-Agent`。  
3. `Authorization` 是否与上游/网关自己的鉴权重复，按你的部署意图保留。

### 3.5 透传跳过的敏感头（`*` 也不会转发）

实现见 `relay/channel/api_request.go` 的 `passthroughSkipHeaderNamesLower`，常见包括：

```
connection, keep-alive, proxy-authenticate, proxy-authorization,
te, trailer, transfer-encoding, upgrade,
cookie, host, content-length, accept-encoding,
authorization, x-api-key, x-goog-api-key,
sec-websocket-key, sec-websocket-version, sec-websocket-extensions
```

（`authorization` 被跳过透传是预期行为；需要渠道 key 时用显式 `"Authorization": "Bearer {api_key}"`。）

---

## 4. 参数覆盖（`param_override`）

字段：渠道编辑 → **Parameter Override**。  
主用途：改 **发给上游的 JSON Body**；附带可改运行时请求头。

### 4.1 两种格式

**旧版 flat（字面顶层 set）：**

```json
{
  "temperature": 0.5,
  "max_tokens": 1000
}
```

**新版 operations（推荐）：**

```json
{
  "operations": [
    {
      "mode": "set",
      "path": "temperature",
      "value": 0.7,
      "conditions": [
        { "path": "model", "mode": "prefix", "value": "gpt" }
      ],
      "logic": "AND"
    }
  ]
}
```

### 4.2 Body 常用 mode

| mode | 说明 |
|---|---|
| `set` | 设 body 字段；`keep_origin: true` 时已存在则跳过 |
| `delete` / `move` / `copy` | 删 / 移 / 复制字段 |
| `prepend` / `append` | 字符串或数组前后追加 |
| `trim_*` / `ensure_*` / `to_lower` / `to_upper` / `replace` / `regex_replace` | 字符串处理 |
| `return_error` | 直接拒绝请求 |
| `prune_objects` | 按条件裁剪对象字段 |
| `sync_fields` | 一侧有值、另一侧缺失时互相同步 |

### 4.3 Header 相关 mode（参数覆盖内）

| mode | 说明 |
|---|---|
| `set_header` | 设置上游请求头；`keep_origin: true` 时目标已有值可跳过 |
| `delete_header` | 删除头 |
| `copy_header` / `move_header` | 复制 / 移动头 |
| `pass_headers` | 把**客户端请求头**（上下文 `request_headers`）按名拷到运行时 `header_override` |

`pass_headers` 实现是 `copyHeaderInContext`：源是客户端 HTTP 头，不是 body 里同名字段。  
客户端没带的头会跳过（不报错）。

```json
{
  "operations": [
    {
      "mode": "pass_headers",
      "value": ["User-Agent", "Session_id", "Originator"],
      "keep_origin": true
    }
  ]
}
```

适合：原样透传客户端部分头，而不是写死固定 UA。

---

## 5. 「按模型条件」是什么意思？

**不是**系统里另有一个开关，而是参数覆盖每条 operation 上的 `conditions`：

- **无 conditions**：每条请求都执行  
- **有 conditions**：只有条件通过才执行（改 body 或 `set_header`）

条件 `path` 常见：

| path | 含义 |
|---|---|
| `model` / `upstream_model` | 映射后的上游模型名 |
| `original_model` | 用户原始模型名 |
| body 字段 | 如 `temperature`、`messages.#` |
| `request_headers.user-agent` | 客户端请求头（小写 key） |

条件 `mode`：`full` / `prefix` / `suffix` / `contains` / `gt` / `gte` / `lt` / `lte`；`invert` 取反；`pass_missing_key` 控制字段缺失时是否当通过。

### 示例：仅 gpt* 才强制 temperature

```json
{
  "operations": [
    {
      "mode": "set",
      "path": "temperature",
      "value": 0.7,
      "conditions": [
        { "path": "model", "mode": "prefix", "value": "gpt" }
      ]
    }
  ]
}
```

| 请求 model | 是否改 temperature |
|---|---|
| `gpt-4o` | 会 |
| `claude-3-5-sonnet` | 不会 |

### 示例：仅模型名含 codex 才固定 UA（参数覆盖路径）

```json
{
  "operations": [
    {
      "mode": "set_header",
      "path": "User-Agent",
      "value": "codex-tui/0.144.3 (Mac OS 15.6.1; arm64) ghostty/1.3.1 (codex-tui; 0.144.3)",
      "conditions": [
        { "path": "model", "mode": "contains", "value": "codex" }
      ]
    }
  ]
}
```

**若渠道所有请求都要固定同一 UA：不要写 conditions，直接用请求头覆盖更简单。**

---

## 6. 两块关系与优先级（简图）

```
客户端 Request
  ├─ Headers ──► header_override 规则（透传 + 显式） ──┐
  │              ▲                                       │
  │              │ 若 param_override 执行了 set_header /   │
  │              │ pass_headers 等，会生成 RuntimeHeaders   │
  │              │ Override，GetEffectiveHeaderOverride     │
  │              │ 优先用运行时结果                         │
  └─ Body ─────► param_override（set/delete/...） ───────┼──► 上游 Request
                                                         │
下游发送时 processHeaderOverride 读 EffectiveHeaderOverride ┘
```

原则：

- **固定改头** → 优先 **请求头覆盖**  
- **改 body / 条件逻辑 / 复杂 header 操作** → **参数覆盖**  
- **不要**用 `set` + path 写成 header 名来「假装改头」

---

## 7. 速查：需求 → 配置

### 7.1 有没有 UA 都固定成同一值

**请求头覆盖：**

```json
{
  "User-Agent": "codex-tui/0.144.3 (Mac OS 15.6.1; arm64) ghostty/1.3.1 (codex-tui; 0.144.3)"
}
```

（可与 `"*": true`、`Authorization` 组合，见 §3.4。）

### 7.2 只透传客户端 UA，没有就不补

**请求头覆盖：**

```json
{
  "User-Agent": "{client_header:User-Agent}"
}
```

或参数覆盖 `pass_headers` + `["User-Agent"]`。

### 7.3 只改 body，永不碰头

只填参数覆盖，且只用 body mode（`set` / `delete` / …），不要用 `*_header` / `pass_headers`。

---

## 8. 与旧笔记的关系

仓库内另有 `2026-07-09T17-30-newapi高级设置解析.md`。  
本文在其基础上补强：

- `set` vs `set_header` 辨析  
- `keep_origin` vs「有则改无则不加」  
- 模型条件（`conditions`）白话说明  
- Codex 固定 UA 推荐配置  
- 修正「参数覆盖与请求头覆盖都是改头」的误解  

若两文冲突，以本文 + 当前代码为准。
