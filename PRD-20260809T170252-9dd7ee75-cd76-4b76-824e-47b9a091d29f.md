# PRD: `/v1/messages` → `/v1/chat/completions` 深度转换

> 分支：`codex/deep-convert-messages2chat`
>
> 范围：仅 Anthropic Messages 入站并最终发往 OpenAI Chat Completions 上游。
>
> 依据：new-api 当前代码链路与 DTO；参考 CC Switch 的兼容目标，不复制其架构。

## 1. 目标

在现有 Claude → OpenAI 转换骨架上，完成 CC Switch 级别的深度字段映射与兼容性加固：

```text
POST /v1/messages
  Anthropic Messages request
→ OpenAI Chat Completions request
→ OpenAI Chat Completions JSON / SSE response
→ Anthropic Messages JSON / SSE response
```

支持文本、图片、thinking、工具声明、工具调用、多轮 `tool_result`、非流式与流式响应。

不新建通用中间表示层或跨协议转换框架；实现收敛在现有 adaptor、`RelayInfo`、`service` converter 和 OpenAI handler 边界。

## 2. 已确认的现有链路

```text
/v1/messages
→ relay.ClaudeHelper
→ channel.Adaptor.ConvertClaudeRequest
→ service.ClaudeToOpenAIRequest
→ openai.Adaptor.ConvertOpenAIRequest
→ OpenAI/Azure Chat Completions endpoint
→ openai.OpenaiHandler / OaiStreamHandler
→ service.ResponseOpenAI2Claude / StreamResponseOpenAI2Claude
```

关键事实：

- `ClaudeToOpenAIRequest` 是共享函数，Messages→Responses、Gemini 中间转换等路径也可调用它，不能无条件加入深转换逻辑。
- `RequestConversionChain` / `FinalRequestRelayFormat` 在 `ConvertClaudeRequest` 返回后才记录，不能作为 converter 内部的请求期判据。
- OpenAI adaptor 负责模型字段规范化与 OpenAI/Azure Chat endpoint 选择，深转换不得复制这些职责。
- Advanced Custom 已有精确 converter：`anthropic_messages_to_openai_chat_completions`。
- 当前 response 与 stream converter 已有 Claude 输出骨架，但不满足严格工具、SSE 终结、错误与 usage 语义。

## 3. 范围与隔离

### 3.1 唯一启用条件

新增深转换规则仅在以下条件同时成立时启用：

1. 入站为 `/v1/messages`，下游格式为 `RelayFormatClaude`；
2. 未使用全局或渠道级 pass-through body；
3. 在调用 `ClaudeToOpenAIRequest` **之前**，调用方已显式冻结最终目标为 OpenAI Chat Completions；
4. 该状态保存在 `RelayInfo` 的请求生命周期内，可由 request converter、non-stream handler、stream converter、finalizer 与 pseudo-SSE fallback 一致读取。

建议新增 fail-closed 专用契约，例如：

```go
AnthropicMessagesToOpenAIChatCompletions bool
```

默认 `false`。不得通过模型名、厂商名、`RelayFormatClaude`、OpenAI-compatible 名称或 `FinalRequestRelayFormat` 反推为 `true`。

### 3.2 可以写入契约的调用点

仅下列明确路径可在请求转换前设为 `true`：

- OpenAI-compatible adaptor 确认将 `/v1/messages` 发往 Chat Completions endpoint 的分支；覆盖普通 OpenAI、Azure、OpenRouter/Xinference 等复用该 adaptor 的渠道；
- Advanced Custom 匹配到 `/v1/messages` route，且 converter **精确等于** `anthropic_messages_to_openai_chat_completions` 的分支。

### 3.3 必须保持 false 的路径

- Messages→Responses，包括 `ShouldChatCompletionsUseResponsesGlobal` / `chatCompletionsViaResponses`；
- Messages→Gemini，以及 Claude→OpenAI DTO→Gemini 的中间转换；
- 原生 Anthropic Messages；
- DeepSeek、Moonshot、Ali 等原生 Messages 路径；
- Advanced Custom 的 `none`、未知或其他 converter；
- 任意 pass-through request body；
- OpenAI、Gemini、Responses 等其他入站协议；
- 非 Claude 下游的 `OpenaiHandler` / `OaiStreamHandler` 调用者。

非目标路径必须走当前 legacy 行为；共享 converter、shared handler 与 finalizer 内部都必须由上述契约守卫。

## 4. 请求转换

本节仅在目标契约为 `true` 时生效。

### 4.1 顶层字段

- 保留既有 `model` 映射、`max_tokens`、`temperature`、`top_p`、`top_k`、`stop_sequences` 与 `stream` 的现有适配责任边界。
- `stop_sequences`：一个值输出 Chat `stop` string；多个值输出 array。
- 有效 tools 存在时：
  - `tool_choice.type=auto` → `"auto"`；
  - `any` → `"required"`；
  - `none` → `"none"`；
  - 指定工具 → `{"type":"function","function":{"name":"..."}}`；
  - `disable_parallel_tool_use` 显式出现时，取反写入 `parallel_tool_calls`。
- 无有效 tools 时，不输出 `tool_choice` 或 `parallel_tool_calls`。
- Anthropic 专有字段如 `metadata`、`service_tier`、`mcp_servers`、`context_management`、`container` 不得泄漏到 Chat request。
- thinking / output effort 仅在目标 adaptor 已有明确能力时映射；普通严格 Chat request 不发送非标准 reasoning 字段。

### 4.2 system

- string system → 单一 `role:"system"` message。
- array system 仅保留非空 text block；普通 Chat 路径用 `"\n"` 连接。
- 不生成空 system message。
- OpenRouter 上游模型为 `anthropic/claude...` 时，保留既有多模态 system content 与 `cache_control` 行为。
- system→developer 的模型专项处理继续属于 OpenAI adaptor，不在 converter 重复实现。

### 4.3 tools 与 schema

- `tools[].input_schema` → `tools[].function.parameters`。
- tools 反序列化失败、tool name 为空、schema 不能表达为 JSON object 均返回转换错误。
- 递归删除值严格为 `"uri"` 的 `format` 字段；覆盖 properties、items、嵌套 object / array。
- 保留 `$schema`、`$defs`、`additionalProperties`、`patternProperties` 等其他 JSON Schema 关键字。
- 不得吞掉序列化、反序列化或 schema 校验错误后发送残缺 tools。

### 4.4 messages、图片、工具回传与 thinking

- string content 原样转换。
- 同一 assistant message 内 text、image、thinking、tool_use 可并存；同时有 text/image 与 tool calls 时必须同时保留。
- `tool_use` → assistant `tool_calls[]`：保留 id、name；input 必须稳定 JSON 序列化为 arguments，失败返回错误。
- 只有 tool calls 而无 text/image 时，assistant content 可为 null；普通空 message 不发送。
- `tool_result` 拆为独立 `role:"tool"` message：
  - `tool_call_id=tool_use_id`；
  - string content 原样写入；array/object 使用 JSON string；序列化失败返回错误；
  - 不依赖 tool name 建立关联，也不应为了兼容性错误插入空 name。
- image：
  - base64 source → `data:{media_type};base64,{data}`；
  - url source → 原 URL；
  - nil source、空 media type/data/URL、未知 source type 都返回明确转换错误，不得 panic。
- 历史 thinking 仅在目标 adaptor 已明确支持的 `reasoning_content` 等字段下发送；普通 Chat 目标不得输出非标准 reasoning 字段。`redacted_thinking` 的降级策略必须固定并由测试锁定。

## 5. 非流式 Chat 响应 → Anthropic 响应

仅在目标契约为 `true` 时使用新增规则。

### 5.1 DTO 与结构校验

最小扩展 Chat response DTO，使 JSON 解码可保留：

- message-level `refusal`；
- legacy `function_call`；
- content array part 的 `type`、`text` / `output_text` 与 `refusal`；
- reasoning / `reasoning_content`。

规则：

- OpenAI error envelope 继续按既有上游错误处理；
- `choices` 为空必须返回 bad response；
- 只使用 `choices[0]`，不得拼接多个 choices。

### 5.2 content、tool 与 stop reason

Anthropic blocks 顺序固定：thinking → text/refusal → tool_use。

- reasoning / reasoning_content → thinking block。
- string content → text block。
- array part 中 text、output_text、refusal，以及 message-level refusal → text block。
- `tool_calls[]` → 多个 tool_use blocks。
- legacy `function_call` 与 `finish_reason:"function_call"` 成套转换为一个 tool_use。
- arguments 只有合法 JSON object 才作为 input；空、非法或非 object 使用 `{}`，并记录 warn。
- stop reason：
  - `stop` → `end_turn`；
  - `length` / `max_tokens` → `max_tokens`；
  - `tool_calls` / `function_call` → `tool_use`；
  - `content_filter` 沿用项目既有 refusal 处理；
  - 未知值记录 warn 并降级为 `end_turn`。

### 5.3 usage

- `prompt_tokens`、`completion_tokens` 映射到 Claude usage。
- cache read 与 cache creation 单独填充，不能重复累计到 `input_tokens`；明确使用 `max(0, prompt_tokens-cache_read-cache_creation)`。
- 缺失上游 usage 时，仍输出非 nil 零值 Claude usage。
- 所有输出 token 数钳制为非负。

## 6. 流式 Chat SSE → Anthropic SSE

仅在目标契约为 `true` 时替换 Claude 专属新增行为；其他调用者保持 legacy stream 语义。

### 6.1 生命周期状态

在 `RelayInfo` 的 Claude request 生命周期状态中扩展：

- 当前打开的 text / thinking / tool blocks；
- 稠密 Anthropic content index；
- upstream tool index → Anthropic index 映射；
- pending tool 的 upstream index、id、name、arguments 缓存与 start 状态；
- first-wins finish reason；
- 最后一个非 nil usage；
- 已发送 error；
- 已发送 success terminal。

`StreamResponseOpenAI2Claude` 仅输出 `message_start` 和 content-block events。它不得输出 `message_delta` 或 `message_stop`。成功 terminal 只能由契约守卫的一个 finalizer 发送一次。

### 6.2 chunk、EOF 与 finalizer

- 保持当前延迟一条 chunk 的读取模型，但 scanner 完成后，最后一个有效 data chunk 必须仍交给转换状态机。
- usage-only chunk 仅更新 usage，不关闭 message。
- finish reason first-wins。
- `[DONE]` 或 EOF 时，finalizer 幂等：关闭已开启 block；无 finish reason 时使用 `end_turn`；发送一次 `message_delta` 与一次 `message_stop`。
- finalizer 不得依赖 `lastStreamData` 能否再解码；空 last data、usage-only 尾块、无 choice 尾块也能从状态完成。
- 已关闭 block 不得再次发送 `content_block_stop`。

### 6.3 thinking、text 与工具

- thinking/text 类型切换前先关闭旧 block，再开启新 block。
- 每个 delta 的全部 `tool_calls[]` 都处理。
- Anthropic index 按可发送 block 首次到达顺序稠密分配，不能复用 upstream index。
- id/name 未完整时缓存 arguments，不能发送不完整 start。
- id 与 name 完整后，发送 tool start 并按到达顺序 flush 缓存 arguments。
- EOF：缺 id 时用基于 upstream tool index 的固定 placeholder；缺 name 的 tool 丢弃并 warn；完整 pending tool 先 start 后 close。

### 6.4 流内错误

- 每个普通 `data:` payload 在解码 Chat chunk 前先检查 OpenAI error envelope。
- error 转成一个 Anthropic `event:error`。
- error 只发送一次；停止继续转换；error 后禁止成功 `message_delta` / `message_stop`。
- scanner callback 与 finalizer 必须共享 error 状态。

保持现有 `StreamScannerHandler` 行扫描架构；使用跨底层 read 边界的 UTF-8 多字节测试验证行为，确认正确前不增加额外字节缓冲层。

## 7. 非流式伪 SSE fallback

仅当目标契约为 `true` 且下游请求非流式时启用。

在 `openai.OpenaiHandler` 读取完整 body 后：首次 JSON 解码失败才尝试 SSE 嗅探。

- 聚合 SSE 中的 content、reasoning、tool calls、finish reason 与最后 usage 为一个 `OpenAITextResponse`；再调用 `ResponseOpenAI2Claude`。
- tool calls 按 upstream index 聚合 id/name/arguments。
- finish reason first-wins。
- 遇到 SSE error envelope 返回上游错误，不得聚合为成功消息。
- 没有有效 choice，或既没有 finish reason 也没有 `[DONE]` / 明确完成标识，按截断或坏响应失败。
- 非目标路径、正常 JSON、OpenAI/Gemini 下游和 pass-through 不得触发该 fallback。

## 8. 预期改动边界

- `relay/common/relay_info.go`：显式 target 契约与 Claude 深转换 stream state。
- `relay/channel/openai/adaptor.go`：在 Chat target 已确认、调用共享 converter 前设置契约。
- `relay/channel/advancedcustom/adaptor.go`：仅精确 converter 设置契约，并维持 pass-through 拒绝。
- `relay/claude_handler.go`：Messages→Responses 分支明确保持契约 false。
- `service/convert.go`：受契约守卫的请求、non-stream 与 stream 深转换。
- `relay/channel/openai/relay-openai.go`、`helper.go`：pseudo-SSE、流式错误、末尾 flush 与唯一 finalizer。
- `dto/openai_request.go`、`dto/openai_response.go`、必要时 `dto/claude.go`：仅增加上述协议字段的最小表达。

不修改其他协议的 request、response、SSE terminal 或 usage 语义。

## 9. 验收与测试

### 隔离

- OpenAI、Azure、OpenRouter/Xinference 的明确 Chat target 及 Advanced Custom 精确 converter 为 true。
- Responses、Gemini 中间链、原生 Messages、Advanced Custom 非目标 converter、pass-through 与未知状态均为 false。
- 对非目标的 request、non-stream、stream chunk、finalizer 回归断言不进入新增分支。

### 请求

覆盖 system、tool_choice、parallel_tool_calls、schema 递归 uri 清理、非法 tool/schema、text/image/tool_use 混合、base64/URL/非法 image、tool_result 多轮与失败序列化、thinking 与模型 token 规范化。

### 非流式

覆盖空 choices、仅第一 choice、thinking/text/refusal/tool order、legacy function_call、全部 finish reason、非法 tool arguments、cache usage、缺失 usage 与负数 clamp。

### 流式

覆盖文本、thinking→text、text→tool、多 tool、首块多 tool、非连续 tool index、arguments 先到、placeholder id、缺 name、finish first-wins、finish 后 usage-only、无 finish 的 DONE/EOF、最后 chunk flush、空尾、单 error、error 后无 success terminal、UTF-8 跨 read 边界。

### 伪 SSE

覆盖非流式请求收到完整 SSE 的 text/reasoning/tools/usage 聚合、SSE error、无 choice、截断、无 completion 标识，以及非目标路径不触发 fallback。

### 质量门槛

- 受影响包测试通过；
- `go build ./...` 通过；
- 所有新增深度规则均由目标契约守卫；
- 回归测试证明非目标路径保持旧行为。
