# Goal: new-api 熔断机制（Circuit Breaker）

> **Codex `/goal` 命令**
> ```
> /goal 在 new-api 中实现进程内存级熔断机制，以 channel_id:model_name 为粒度，
> 连续非 2xx 失败达到阈值即熔断、冷却后半开试探、成功恢复。
> 验证方式：circuitbreaker 包单元测试全部通过 + 现有 auto-ban 渠道回归测试无退化。
> 约束：不修改 auto-ban 逻辑、不写 DB/Redis、不影响计费/预扣费/日志。
> 边界：仅修改 service/circuitbreaker/（新建）、setting/operation_setting/、dto/channel_settings.go、
> controller/relay.go、model/channel_cache.go。
> 迭代策略：每完成一个文件的修改后运行 go build ./... 确认编译通过，再继续下一个。
> 若阻塞：发现循环依赖或现有测试失败，立即停止并报告 blocker + 证据。
> ```

---

## Overview

在现有 auto-ban 机制之外，新增一个**临时、轻量的进程内存熔断器**。auto-ban 与熔断互斥（`auto_ban=1`→auto-ban，`auto_ban=0`→熔断）。熔断以 `channel_id:model_name` 为 key，状态机为 CLOSED→OPEN→HALF_OPEN→CLOSED，全程不写数据库/Redis，仅进程内存。

**Motivation**: 现有 auto-ban 触发条件过窄（仅 401+关键词匹配），禁用持久化写 DB，恢复依赖偶然的后续请求成功，且无时间窗口——单个 401 即禁用，过于激进。

---

## Requirements

### Functional

- **MUST** 以 `channel_id:model_name` 为熔断粒度，group 不参与 key 计算
- **MUST** 实现三态状态机：CLOSED（正常）→ OPEN（熔断）→ HALF_OPEN（试探）→ CLOSED
- **MUST** 连续非 2xx 失败次数达到阈值（默认 3）时从 CLOSED 进入 OPEN
- **MUST** 中间任意一次成功（2xx）重置失败计数器为 0
- **MUST** OPEN 状态持续冷却时间（默认 60s）后自动转为 HALF_OPEN
- **MUST** HALF_OPEN 状态下仅允许一个试探请求通过，成功→CLOSED，失败→OPEN（重置冷却）
- **MUST** 渠道选择阶段（`GetRandomSatisfiedChannel`）排除 OPEN 状态的渠道+模型
- **MUST** 所有该模型的渠道都 OPEN 时，返回错误给客户端（不降级随机选）
- **MUST** 仅在 `auto_ban=0` 时启用熔断，`auto_ban=1` 时走现有 auto-ban 逻辑
- **MUST** 手动禁用渠道（Status=2）不受两种机制影响

### Configuration

- **MUST** 支持全局默认配置：`circuit_breaker_failure_threshold`（默认 3）、`circuit_breaker_cooldown_seconds`（默认 60）
- **MUST** 支持渠道级覆盖：通过 `other_settings` JSON 字段覆盖全局默认值
- **MUST** 渠道覆盖优先级高于全局默认

### Non-functional

- **MUST** 状态存储仅在进程内存（map + sync.RWMutex），不写 DB、不写 Redis
- **MUST NOT** 修改现有 auto-ban 逻辑
- **MUST NOT** 影响计费、预扣费、日志记录等现有流程
- **MUST NOT** 做滑动窗口计数（仅连续失败计数）
- **MUST NOT** 做多实例同步
- **SHOULD** 在 `AllowProbe` 中处理试探请求超时（30s 无响应自动重置 `probeInFlight`）

### Verification

- **MUST** 提供 `service/circuitbreaker/` 包的单元测试，覆盖所有状态转换
- **MUST** 现有 auto-ban 渠道（`auto_ban=1`）的回归测试无退化
- **MUST** `go build ./...` 编译通过，无循环依赖
- **SHOULD** 集成测试：模拟连续失败触发熔断，验证 OPEN 期间请求不被路由

---

## Design

### Architecture

```
controller/relay.go (retry loop)
  ├── [渠道选择前] model.GetRandomSatisfiedChannel()
  │     └── circuitbreaker.IsOpen() 过滤 OPEN 渠道
  ├── [请求完成后] newAPIError == nil
  │     └── circuitbreaker.RecordSuccess() 重置计数
  └── [请求失败后] processChannelError()
        └── circuitbreaker.RecordFailure() 递增失败计数
```

### File Plan

| # | File | Action | Purpose |
|---|------|--------|---------|
| 1 | `service/circuitbreaker/circuit_breaker.go` | **CREATE** | 熔断状态管理器（核心） |
| 2 | `service/circuitbreaker/circuit_breaker_test.go` | **CREATE** | 单元测试 |
| 3 | `setting/operation_setting/circuit_breaker_setting.go` | **CREATE** | 全局默认配置 |
| 4 | `dto/channel_settings.go` | MODIFY | 新增 `CircuitBreakerFailureThreshold`、`CircuitBreakerCooldownSeconds` 字段 |
| 5 | `controller/relay.go` | MODIFY | 成功/失败时调用 circuitbreaker 记录；参见介入点 A/B |
| 6 | `model/channel_cache.go` | MODIFY | `GetRandomSatisfiedChannel` 中过滤 OPEN 渠道 |

### State Machine

```
    连续失败 >= 阈值
CLOSED ─────────────────> OPEN
  ↑                        │
  │   试探成功(2xx)         │ 冷却时间到期
  │                        ↓
  └── HALF_OPEN <──────── OPEN（到期自动转换）
       │
       │ 试探失败(非2xx)
       └──> OPEN（重置冷却时间）
```

### Data Model

```go
// service/circuitbreaker/circuit_breaker.go

type State int
const (
    StateClosed   State = iota
    StateOpen
    StateHalfOpen
)

type entry struct {
    state         State
    failCount     int
    openedAt      time.Time
    probeInFlight bool
    probeSince    time.Time
}

// key = fmt.Sprintf("%d:%s", channelId, modelName)
var states = map[string]*entry{}
var mu sync.RWMutex
```

### Public API

| Function | Description |
|----------|-------------|
| `IsOpen(channelId int, modelName string) bool` | 查询是否 OPEN（惰性检查冷却到期→HALF_OPEN） |
| `AllowProbe(channelId int, modelName string) bool` | HALF_OPEN 时是否允许试探（仅一个） |
| `RecordSuccess(channelId int, modelName string)` | 记录成功，状态→CLOSED，计数归零 |
| `RecordFailure(channelId int, modelName string, statusCode int)` | 记录失败，状态机转换 |
| `GetFailureThreshold(channelId int, modelName string, settings *dto.ChannelOtherSettings) int` | 获取阈值 |
| `GetCooldownSeconds(channelId int, modelName string, settings *dto.ChannelOtherSettings) int` | 获取冷却时间 |

### Intervention Points in `controller/relay.go`

**Point A — Success** (retry loop, after `newAPIError == nil`):
```go
if newAPIError == nil {
    if !channel.GetAutoBan() {
        circuitbreaker.RecordSuccess(channel.Id, relayInfo.OriginModelName)
    }
    relayInfo.LastError = nil
    return
}
```

**Point B — Failure** (after `processChannelError` call):
```go
processChannelError(c, channelError, newAPIError)
if !channel.GetAutoBan() {
    circuitbreaker.RecordFailure(channel.Id, relayInfo.OriginModelName, newAPIError.StatusCode)
}
```

### Channel Selection Filter in `model/channel_cache.go`

In `GetRandomSatisfiedChannel`, when iterating candidate channels in the weighted random selection loop, add:
```go
if !channel.GetAutoBan() && circuitbreaker.IsOpen(channel.Id, model) {
    continue // skip OPEN channels
}
if !channel.GetAutoBan() && circuitbreaker.IsHalfOpen(channel.Id, model) && !circuitbreaker.AllowProbe(channel.Id, model) {
    continue // skip HALF_OPEN channels that don't get probe right
}
```

The `model` parameter is already available as the function argument.

### Configuration Priority

```
Channel OtherSettings (circuit_breaker_failure_threshold / circuit_breaker_cooldown_seconds)
  ↓ fallback
Global OperationSetting (CircuitBreakerSetting.FailureThreshold / CooldownSeconds)
  ↓ fallback
Hardcoded defaults (3 / 60)
```

---

## Deliverables

### Concrete Files

1. `service/circuitbreaker/circuit_breaker.go` — ~150 lines, state manager + public API
2. `service/circuitbreaker/circuit_breaker_test.go` — ~200 lines, table-driven tests
3. `setting/operation_setting/circuit_breaker_setting.go` — ~30 lines, config struct + init
4. `dto/channel_settings.go` — +4 lines, two new fields in `ChannelOtherSettings`
5. `controller/relay.go` — +8 lines, two intervention points
6. `model/channel_cache.go` — +8 lines, filter in `GetRandomSatisfiedChannel`

### Commands to Run

```bash
# Build check
go build ./...

# Unit tests
go test ./service/circuitbreaker/ -v -count=1

# Regression tests (auto-ban)
go test ./controller/ -run TestChannel -v -count=1

# Full test suite
go test ./... -count=1 2>&1 | tail -20
```

### Expected Receipt

- `go build ./...` → exit code 0
- `go test ./service/circuitbreaker/ -v` → 7+ tests PASS
- All existing tests → PASS (no regression)
- Auto-ban channels (`auto_ban=1`) → behavior unchanged

---

## Open Questions

- 是否需要为熔断事件添加日志（SysLog）？需求文档未明确要求，建议在状态转换时记录 Debug 级别日志，方便排查。
- `AllowProbe` 超时重置（30s）是否合理？需根据实际请求耗时分布调整。