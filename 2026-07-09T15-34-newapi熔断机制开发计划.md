# 熔断机制实现计划

## 上下文

**问题**: 现有 auto-ban 机制触发条件太窄（仅 401+关键词），禁用是持久化的（写 DB），没有时间窗口。需要增加一个临时、轻量的进程内存熔断机制。

**互斥关系**: `auto_ban=1`→auto-ban，`auto_ban=0`→熔断，两者不可同时生效。

**核心设计**: 以 `channel_id:model_name` 为 key，连续非 2xx 失败 >= 阈值即熔断，冷却时间后进入半开试探，成功则恢复。全程在进程内存中，不写 DB/Redis。

## 涉及文件

### 新建文件

1. **`service/circuitbreaker/circuit_breaker.go`** — 熔断状态管理器
   - 包级单例，通过 `sync.RWMutex` 保护
   - 数据结构：`map[string]*State`，key = `"<channel_id>:<model_name>"`
   - 状态机：CLOSED → OPEN → HALF_OPEN → CLOSED
   - 公开 API：
     - `IsOpen(channelId int, modelName string) bool` — 查询是否 OPEN（渠道选择前调用）
     - `IsHalfOpen(channelId int, modelName string) bool` — 查询是否 HALF_OPEN
     - `AllowProbe(channelId int, modelName string) bool` — HALF_OPEN 时只允许一个试探请求通过，返回 true 表示本请求获得试探权
     - `RecordSuccess(channelId int, modelName string)` — 记录成功，状态→CLOSED
     - `RecordFailure(channelId int, modelName string, statusCode int)` — 记录失败，根据状态机转换
     - `GetFailureThreshold(channelId int, modelName string, channelSettings *dto.ChannelOtherSettings) int` — 获取阈值（渠道覆盖优先于全局默认）
     - `GetCooldownSeconds(channelId int, modelName string, channelSettings *dto.ChannelOtherSettings) int` — 获取冷却时间
   - 内部逻辑：
     - CLOSED + 失败（非 2xx）→ 计数器+1；达到阈值→OPEN，记录 openedAt
     - CLOSED + 成功（2xx）→ 计数器重置为 0
     - OPEN + 冷却时间到期 → 自动转为 HALF_OPEN（在 `IsOpen` 查询时惰性检查）
     - HALF_OPEN + 试探成功 → CLOSED，计数归零
     - HALF_OPEN + 试探失败 → OPEN，重置冷却时间
   - 注意：`IsOpen` 需要在查询时惰性检查冷却时间是否到期，到期则自动转为 HALF_OPEN

2. **`setting/operation_setting/circuit_breaker_setting.go`** — 全局默认配置
   - 结构体 `CircuitBreakerSetting`：
     - `FailureThreshold int` — 默认 3
     - `CooldownSeconds int` — 默认 60
   - 注册到 `config.GlobalConfig.Register("circuit_breaker_setting", &setting)`
   - 提供 `GetCircuitBreakerSetting()` 访问函数

### 修改文件

3. **`dto/channel_settings.go`** — 渠道级覆盖参数
   - 在 `ChannelOtherSettings` 结构体中新增两个字段：
     ```go
     CircuitBreakerFailureThreshold *int `json:"circuit_breaker_failure_threshold,omitempty"`
     CircuitBreakerCooldownSeconds  *int `json:"circuit_breaker_cooldown_seconds,omitempty"`
     ```
   - 使用指针类型以区分"未设置"（nil）和"设置为 0"

4. **`controller/relay.go`** — 熔断介入点（三个位置）

   **位置 A**：成功时记录（retry loop 内，`newAPIError == nil` 时）
   - 在 `if newAPIError == nil { relayInfo.LastError = nil; return }` 之前，当 `!channel.GetAutoBan()` 时：
     ```go
     circuitbreaker.RecordSuccess(channel.Id, relayInfo.OriginModelName)
     ```

   **位置 B**：失败时记录（`processChannelError` 调用处）
   - 在 `processChannelError(...)` 调用之后，当 `!channel.GetAutoBan()` 时：
     ```go
     circuitbreaker.RecordFailure(channel.Id, relayInfo.OriginModelName, newAPIError.StatusCode)
     ```

   **位置 C**：半开试探通过时标记（`processChannelError` 调用前，不对 channel error 进行 auto-ban）
   - 当 `!channel.GetAutoBan()` 且 `circuitbreaker.IsHalfOpen(channel.Id, relayInfo.OriginModelName)` 时，若试探失败则不会走 auto-ban 逻辑（因为 `AutoBan=0` 已经不触发 auto-ban），失败会在位置 B 被记录

5. **`model/channel_cache.go`** — 渠道选择时过滤 OPEN 渠道
   - 在 `GetRandomSatisfiedChannel` 函数中，遍历候选 channels 时，对每个 channel 检查：
     - 如果 `channel.GetAutoBan() == false`（即熔断模式），且 `circuitbreaker.IsOpen(channel.Id, model)` 为 true
     - 则跳过该 channel，不加入候选列表
   - 同时检查 HALF_OPEN 状态：如果 `IsHalfOpen` 且 `AllowProbe` 返回 false（已有试探请求在进行），也跳过
   - 模型名 `model` 是函数参数，直接可用

## 实现细节

### 熔断 key 格式
```
fmt.Sprintf("%d:%s", channelId, modelName)
```
例如 `"42:gpt-4o"`。注意：modelName 需要使用规范化后的名称（即 `GetRandomSatisfiedChannel` 中已经尝试过 `FormatMatchingModelName` 的版本）。

### 状态机惰性转换
`IsOpen` 查询时：
1. 如果状态是 OPEN 且 `time.Since(openedAt) >= cooldown`，则自动转为 HALF_OPEN，返回 false（不阻塞）
2. 如果状态是 HALF_OPEN 且 `AllowProbe` 已返回过 true（有试探请求在进行），返回 true（阻塞后续请求）
3. 如果状态是 OPEN 且冷却未到期，返回 true（阻塞）

### 试探请求协调
HALF_OPEN 状态需要保证只有一个试探请求通过。使用一个 `probeInFlight bool` 字段：
- `AllowProbe`: 如果 `!probeInFlight`，设置 `probeInFlight = true`，返回 true
- 试探成功/失败后，`probeInFlight` 重置为 false
- 如果试探请求超时（没有调用 RecordSuccess/RecordFailure），probeInFlight 会一直为 true，需要超时机制：在 `AllowProbe` 中检查 `probeInFlight` 是否超过一定时间（如 30s），超时则重置

### 计数重置
- 成功（2xx）→ 计数归零，状态→CLOSED
- 连续失败达到阈值→OPEN
- 中间任意一次成功→计数归零
- 注意：只有完全的成功（StatusCode 在 [200,300)）才重置计数，其他情况（包括无状态码的请求失败）都算失败

### 与现有重试循环的协作
现有的 retry loop 在 `shouldRetry` 返回 false 或 `getChannel` 返回 nil 时退出。熔断过滤后，如果所有渠道都 OPEN，`GetRandomSatisfiedChannel` 返回 nil，natural 地触发"无可用渠道"错误返回给客户端。

## 验证

1. **单元测试** (`service/circuitbreaker/circuit_breaker_test.go`)：
   - 测试 CLOSED→OPEN 转换（连续失败达到阈值）
   - 测试成功重置计数
   - 测试 OPEN→HALF_OPEN（冷却时间到期）
   - 测试 HALF_OPEN→CLOSED（试探成功）
   - 测试 HALF_OPEN→OPEN（试探失败）
   - 测试 `AllowProbe` 互斥（同时只有一个试探请求）
   - 测试配置优先级（渠道覆盖 > 全局默认）

2. **集成测试**：
   - 启动服务，配置一个 `auto_ban=0` 的渠道
   - 模拟连续 3 次非 2xx 请求，验证该渠道+模型进入 OPEN 状态
   - 验证 OPEN 期间请求不会路由到该渠道
   - 等待冷却时间后，验证下一次请求进入 HALF_OPEN
   - 验证 HALF_OPEN 试探成功后恢复 CLOSED

3. **回归测试**：
   - 确认 `auto_ban=1` 的渠道不受影响
   - 确认手动禁用（Status=2）不受影响
   - 确认计费/预扣费/日志记录流程不受影响

## 不涉及

- 前端变更（第一版不做可视化）
- 修改现有 auto-ban 逻辑
- Redis/DB 持久化
- 滑动窗口计数
- 多实例同步