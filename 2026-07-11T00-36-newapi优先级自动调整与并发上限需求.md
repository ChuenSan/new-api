# new-api 渠道优先级自动调整 & 并发上限 需求文档

## 概述

本文档记录了 new-api 的渠道优先级自动调整（Priority Auto-Adjustment）、并发上限（Concurrency Limit）和动态自适应并发（Adaptive Concurrency）三个功能的完整需求。

---

## 一、优先级自动调整（Priority Auto-Adjustment）

### 1.1 功能描述

系统根据渠道的连续成功/失败次数，自动调整渠道优先级。当渠道连续成功达到阈值时，优先级 +1；连续失败达到阈值时，优先级 -1。

### 1.2 详细决策

| # | 决策点 | 结论 |
|---|--------|------|
| 1 | 调整方式 | 持久化写入数据库（`channels` 表 `priority` 字段） |
| 2 | 调整粒度 | per-channel（按渠道整体），非 per-model |
| 3 | 功能开关 | 全局开关 `auto_priority_enabled`，默认关闭 |
| 4 | 计数器存储 | `channels` 表新增两个字段：`auto_priority_success_count`（INT，默认 0）、`auto_priority_failure_count`（INT，默认 0） |
| 5 | 计数器溢出 | 触发阈值后计数器归零，不存在溢出问题 |
| 6 | 优先级上限 | 不超过全局最高优先级的 2 倍 |
| 7 | 优先级下限 | -999（与前端 NumericSpinnerInput min 一致） |
| 8 | 成功定义 | HTTP 2xx 状态码 |
| 9 | 429 处理 | 429 不计入失败计数（429 是限流不是错误） |
| 10 | 计数器重置 | 触发调整后，成功和失败计数器**对称归零** |
| 11 | 失败打断成功 | 连续成功中任意一次失败，成功计数器归零，失败计数器 +1 |
| 12 | 成功阈值 | 连续 20 次成功 → 优先级 +1（全局可配） |
| 13 | 失败阈值 | 连续 5 次失败 → 优先级 -1（全局可配） |
| 14 | 生效范围 | 所有渠道（`auto_ban=0` 和 `auto_ban=1` 均生效） |
| 15 | 缓存刷新 | 调整后立即调用 `InitChannelCache()` 刷新缓存 |
| 16 | 审计日志 | 记录，action=`channel.priority_auto_adjust`，区分于手动调整 |
| 17 | 手动改优先级 | 手动修改优先级后，自动计数器重置为 0 |
| 18 | 调整时机 | 每次 relay 请求完成后，判断连续成功/失败是否达到阈值 |

### 1.3 全局设置项

| 设置项 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| `auto_priority_enabled` | bool | `false` | 优先级自动调整总开关 |
| `auto_priority_success_threshold` | int | `20` | 连续成功触发阈值 |
| `auto_priority_failure_threshold` | int | `5` | 连续失败触发阈值 |

### 1.4 编辑器字段

`channels` 表新增字段：

| 字段名 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| `auto_priority_success_count` | int | `0` | 连续成功计数 |
| `auto_priority_failure_count` | int | `0` | 连续失败计数 |

### 1.5 状态机

```
初始状态: success_count=0, failure_count=0

请求成功 (2xx):
  success_count++
  failure_count = 0
  若 success_count >= success_threshold:
    priority = min(priority + 1, max_priority * 2)
    success_count = 0
    记录审计日志

请求失败 (非2xx, 非429):
  failure_count++
  success_count = 0
  若 failure_count >= failure_threshold:
    priority = max(priority - 1, -999)
    failure_count = 0
    记录审计日志

请求 429:
  不改变任何计数器
```

---

## 二、并发上限（Concurrency Limit）

### 2.1 功能描述

为每个渠道设置并发请求上限，当渠道正在处理的请求数达到上限时，跳过该渠道，选择下一个可用渠道。用于解决高可用渠道因并发过高导致频繁 429 的问题。

### 2.2 详细决策

| # | 决策点 | 结论 |
|---|--------|------|
| 1 | 语义 | 同一时刻该渠道**正在处理的请求数** ≤ N |
| 2 | 粒度 | per-channel，所有 model 共享同一个并发池 |
| 3 | 存储 | `channels` 表新增字段 `max_concurrency` |
| 4 | 类型 | `*int`，`nil` 或 `0` 表示不限制 |
| 5 | 计数器实现 | 纯内存 `sync.Map` + atomic counter，进程重启归零 |
| 6 | 取值范围 | 0 ~ 9999 |
| 7 | 并发满了 | 直接跳过该渠道，选下一个（与熔断器 OPEN 行为一致） |
| 8 | 计数 +1 时机 | 选中渠道、准备发送请求时 |
| 9 | 计数 -1 时机 | 收到上游响应后立即 -1（不等待计费/日志等本地处理） |
| 10 | 生效范围 | 所有渠道（`auto_ban=0` 和 `auto_ban=1` 均生效） |
| 11 | 与熔断器交互 | 熔断器优先检查 → 再检查并发上限 |
| 12 | HALF_OPEN probe | probe 请求占用一个并发槽位 |
| 13 | 全局默认值 | `default_max_concurrency`，默认 0（不限） |

### 2.3 编辑器字段

`channels` 表新增字段：

| 字段名 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| `max_concurrency` | `*int` | `nil` | 并发上限，nil/0=不限 |

### 2.4 全局设置项

| 设置项 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| `default_max_concurrency` | int | `0` | 全局默认并发上限，0=不限 |

### 2.5 检查流程

在 `GetRandomSatisfiedChannel` 的加权随机选择循环中，每个候选渠道在进入权重计算前：

```
1. 熔断器 OPEN → 跳过
2. 熔断器 HALF_OPEN 且 probe slot 被占用 → 跳过
3. 并发计数 >= max_concurrency → 跳过
4. 通过 → 进入权重随机选择，并发计数 +1
```

---

## 三、动态自适应并发（Adaptive Concurrency）

### 3.1 功能描述

动态自适应并发在手动并发上限（`max_concurrency`）的基础上，通过 AIMD 算法自动学习渠道的最优并发值。当渠道开启自适应后，系统以 `max_concurrency` 为天花板，在实际运行中动态调整并发限制：成功时缓慢增加（+1），遇到 429 时快速回退（减半），最终收敛到上游实际能承受的并发水平。

### 3.2 与手动并发上限的关系

- **手动并发上限**（`max_concurrency`）作为**天花板**（硬上限），自适应值永远不会超过它
- 动态自适应在天花板之下自动浮动
- 如果用户不想自适应，设 `max_concurrency` 即为固定上限（`adaptive_concurrency_enabled=false`）
- 两个功能可以一起上线，代码分两层：基础层（手动上限）+ 自适应层（AIMD）

### 3.3 详细决策

| # | 决策点 | 结论 |
|---|--------|------|
| 1 | 与手动上限关系 | 互补，`max_concurrency` 为天花板，自适应值在天花板之下浮动 |
| 2 | 功能开关 | per-channel 开关 `adaptive_concurrency_enabled`，默认关闭 |
| 3 | 算法 | AIMD（Additive Increase, Multiplicative Decrease） |
| 4 | 粒度 | per-channel，所有 model 共享 |
| 5 | 初始值 | 从天板 `max_concurrency` 开始；若天花板为 0（不限），则从 5 开始 |
| 6 | 增加步长 | 每次成功请求 +1 |
| 7 | 减少幅度 | 遇到 429 时减半（floor），最小值 2 |
| 8 | 冷却期 | 无，AIMD 自然收敛 |
| 9 | 存储 | 纯内存，进程重启后从初始值重新学习 |
| 10 | 429 检测 | 收到上游 429 响应时触发减半；其他 5xx 不影响 |
| 11 | 日志 | 减半时记录 SysLog，普通 +1 不记录 |
| 12 | 前端展示 | 渠道列表显示"当前值 / 天花板"（如 "12 / 50"） |
| 13 | 当前值获取 | 渠道列表 API 返回时附加 `adaptive_concurrency_current` 字段 |

### 3.4 编辑器字段

`channels` 表新增字段：

| 字段名 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| `adaptive_concurrency_enabled` | `*bool` | `false` | 是否开启自适应并发 |

### 3.5 AIMD 算法状态机

```
初始状态: current = max_concurrency ?? 5

请求成功:
  current = min(current + 1, max_concurrency)  // 天花板之下缓慢爬升

请求 429:
  current = max(floor(current / 2), 2)          // 快速回退，至少保留 2
  SysLog: "channel #X adaptive concurrency halved: {old} → {new} due to 429"

收敛示例:
  天花=50, 初始=50 → 429 → 25 → 一次成功 → 26 → ...
  若持续 429: 50→25→12→6→3→2（5次429收敛到最小值）
  之后成功: 2→3→4→...→慢慢爬回平衡点
```

### 3.6 检查流程（更新）

在 `GetRandomSatisfiedChannel` 的加权随机选择循环中：

```
1. 熔断器 OPEN → 跳过
2. 熔断器 HALF_OPEN 且 probe slot 被占用 → 跳过
3. 计算有效并发上限:
   - 若 adaptive_concurrency_enabled=true: effective = min(adaptive_current, max_concurrency)
   - 若 adaptive_concurrency_enabled=false: effective = max_concurrency (0=不限)
4. 并发计数 >= effective → 跳过
5. 通过 → 进入权重随机选择，并发计数 +1
```

---

## 四、前端 UI

### 4.1 渠道列表列顺序

新列顺序（仅 `web/default`）：

```
... → Group → Concurrency → Priority → Weight → ...
```

Concurrency 列展示逻辑：
- `adaptive_concurrency_enabled=false`：显示固定值，如 "0"（不限）或 "50"
- `adaptive_concurrency_enabled=true`：显示 "当前值 / 天花板"，如 "12 / 50"，tooltip 显示"自适应模式"
- 值为 0 或 nil：显示 "—"（不限）

### 4.2 渠道编辑表单

在 Priority 和 Group 字段之间增加：
- `max_concurrency` — `NumericSpinnerInput`，min=0, max=9999
- `adaptive_concurrency_enabled` — Switch 开关，仅当 `max_concurrency` > 0 时可开启

### 4.3 系统设置页

在 `Routing Reliability` 板块（`web/default/src/features/system-settings/models/routing-reliability-section.tsx`）新增三个子区域：

**Priority Auto-Adjustment 子区域：**
- `auto_priority_enabled` — Switch 开关
- `auto_priority_success_threshold` — Number Input（min=1）
- `auto_priority_failure_threshold` — Number Input（min=1）

**Concurrency Limit 子区域：**
- `default_max_concurrency` — Number Input（min=0, max=9999）

**Adaptive Concurrency 子区域：**
- 无全局设置项（per-channel 开关，默认关闭）

---

## 五、涉及文件

### 后端

| 文件 | 变更 |
|------|------|
| `model/channel.go` | `Channel` struct 新增 `AutoPrioritySuccessCount`、`AutoPriorityFailureCount`、`MaxConcurrency`、`AdaptiveConcurrencyEnabled` 字段 |
| `model/channel_cache.go` | `GetRandomSatisfiedChannel` 增加并发上限检查（含自适应逻辑） |
| `service/adaptive_concurrency.go` | **新增**：AIMD 算法、并发计数器、429 回调 |
| `service/circuitbreaker/circuit_breaker.go` | 无变更（并发检查在 channel_cache 层） |
| `service/channel_select.go` | 无变更（并发检查在 channel_cache 层） |
| `controller/relay.go` | relay 完成时调用优先级自动调整 + 自适应并发回调 |
| `controller/channel.go` | `UpdateChannel` 手动改优先级时重置计数器；返回结构增加 `adaptive_concurrency_current` |
| `setting/operation_setting/` | 新增全局设置结构体（auto_priority、default_max_concurrency） |
| `dto/channel_settings.go` | 无变更（新字段为 Channel 一级字段） |

### 前端（仅 `web/default`）

| 文件 | 变更 |
|------|------|
| `features/channels/components/channels-columns.tsx` | 新增 `ConcurrencyCell`（含自适应展示），调整列顺序 |
| `features/channels/hooks/use-channel-mutate-form.ts` | 编辑表单增加 `max_concurrency`、`adaptive_concurrency_enabled` 字段 |
| `features/system-settings/models/routing-reliability-section.tsx` | 新增三个子区域 |
| `i18n/locales/en.json` | 新增相关翻译 key |
| `i18n/locales/zh.json` | 新增相关翻译 key |