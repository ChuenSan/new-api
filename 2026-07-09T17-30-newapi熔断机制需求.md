# 熔断机制需求文档

> 创建时间：2026-07-09T17:30

## 1. 背景

当前系统已有 auto-ban 自动禁用机制：渠道返回特定错误（401 或匹配关键词如余额不足）时，自动将渠道状态设为 `ChannelStatusAutoDisabled=3` 并写入数据库，cache sync 后从可用池移除。

auto-ban 的问题：
- 触发条件太窄——仅 401 + 关键词匹配，上游临时故障（502/503/429/超时等）不会触发
- 禁用是持久化的——写 DB 改状态，恢复依赖偶然的后续请求成功
- 没有时间窗口——单个 401 就禁用，过于激进

## 2. 需求

在 auto-ban 之外，增加一个**临时、轻量的熔断机制（Circuit Breaker）**。使用者可在 auto-ban 和熔断之间二选一。

## 3. 核心设计

### 3.1 互斥关系

| 渠道 `auto_ban` 值 | 生效机制 |
|---|---|
| `1`（开启） | 走现有 auto-ban 逻辑，熔断不可用 |
| `0`（关闭） | 走熔断机制，auto-ban 不可用 |

手动禁用渠道（Status=2）不受影响，两种机制下均可手动禁用。

### 3.2 熔断粒度

**渠道 + 模型**：`channel_id:model_name` 为熔断 key。

- 渠道 42 的 `gpt-4o` 熔断了，不影响渠道 42 的 `gpt-4o-mini`
- group 不参与熔断 key

### 3.3 触发条件

同一 `channel_id:model_name` **连续**非 2xx 失败达到阈值（默认 3 次），触发熔断。

- "连续失败"：中间有任意一次成功（2xx）则计数器重置为 0
- "非 2xx"：上游返回的 HTTP 状态码不在 [200, 300) 区间，或请求本身失败（连接超时、DNS 错误等）

### 3.4 状态机

```
     连续失败 >= 阈值
CLOSED ─────────────────> OPEN
  ↑                        │
  │   试探成功              │ 冷却时间到期
  │                        ↓
  └── HALF_OPEN <──────── OPEN（到期自动转换）
       │
       │ 试探失败
       └──> OPEN（重置冷却时间）
```

- **CLOSED**（关闭）：正常状态，请求正常发出，失败计数
- **OPEN**（打开/熔断）：拒绝请求，该渠道+模型从候选池排除。持续冷却时间（默认 60 秒）
- **HALF_OPEN**（半开）：冷却时间到期后自动进入，下一个请求获得试探机会
  - 试探成功（2xx）→ CLOSED，重置计数
  - 试探失败（非 2xx）→ OPEN，重置冷却时间

### 3.5 熔断期间行为

- 渠道选择阶段（`GetRandomSatisfiedChannel`）排除处于 OPEN 状态的渠道+模型
- 所有该模型的渠道都 OPEN 时，返回错误给客户端（不降级随机选一个）

### 3.6 状态存储

**进程内存**（map），不写数据库，不写 Redis。单实例部署不需要跨节点同步。

## 4. 可配置参数

### 4.1 参数列表

| 参数 | 含义 | 全局默认值 | 渠道可覆盖 |
|---|---|---|---|
| `circuit_breaker_failure_threshold` | 触发熔断的连续失败次数 | 3 | 是，`other_settings` |
| `circuit_breaker_cooldown_seconds` | 熔断冷却时间（秒） | 60 | 是，`other_settings` |

### 4.2 配置来源

- **全局默认**：系统设置（`operation_setting` 或环境变量）
- **渠道覆盖**：渠道的 `other_settings`（JSON 字段 `settings`），优先级高于全局默认

## 5. 不做什么

- 不做滑动窗口计数，只做连续失败计数
- 不做 Redis/DB 持久化
- 不做多实例同步
- 不做前端熔断状态可视化（第一版）
- 不修改现有 auto-ban 逻辑
- 不影响计费、预扣费、日志记录等现有流程

## 6. 实现要点

### 6.1 代码涉及模块

- 新增：熔断状态管理器（内存 map + 读写锁）
- `controller/relay.go` — 渠道选择前后加入熔断检查，请求完成后记录成功/失败
- `setting/operation_setting/` — 全局默认参数
- `dto/channel_other_settings.go` — 渠道级覆盖参数
- `model/channel.go` — `AutoBan=0` 时启用熔断

### 6.2 介入点

1. **渠道选择前**：`GetRandomSatisfiedChannel` 或外层调用处，过滤 OPEN 状态的渠道+模型
2. **请求完成后**：`relayHandler` 返回后（在现有重试循环内），根据 `newAPIError` 的 status code 更新熔断状态
3. **半开试探**：过滤时识别 HALF_OPEN 状态，允许一个请求通过，并标记为试探请求