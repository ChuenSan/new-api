# PRD：Anthropic Messages → OpenAI Chat Completions 深度转换

> 文档 ID：`20260809T165527-27135c3a-2ab4-45c2-b0b2-dee61e468431`
> 目标分支：`codex/deep-convert-messages2chat`
> 代码基线：当前分支工作区源码
> 权威依据：new-api 当前代码；参考 CC Switch 思路但不复制其架构

## 1. 结论与目标

new-api 已具备 `/v1/messages` 接收、Claude 请求转 OpenAI 请求、Chat Completions 上游调用以及 OpenAI 响应转 Anthropic 响应的基础链路。本任务不新建通用协议转换层，而是在现有链路上补齐深度字段映射、流式状态管理、错误处理和边界隔离。

唯一目标方向：

```text
客户端：POST /v1/messages（Anthropic Messages）
                    ↓
new-api：现有 Claude → OpenAI 转换链路
                    ↓
上游：POST /v1/chat/completions（OpenAI Chat Completions）
```

完成后，严格 Anthropic Messages 客户端应能通过该方向稳定使用文本、图片、thinking、工具调用、工具结果回传、非流式响应和流式响应。

## 2. 当前代码事实

### 2.1 已存在的主链路

1. `/v1/messages` 被标记为 Claude relay format：`router/relay-router.go:86-89`。
2. `ClaudeHelper` 完成模型映射、渠道 adaptor 选择和请求分派：`relay/claude_handler.go:23-47`、`relay/claude_handler.go:134-165`。
3. OpenAI adaptor 调用 `service.ClaudeToOpenAIRequest`，随后进入 `ConvertOpenAIRequest`：`relay/channel/openai/adaptor.go:52-82`。
4. OpenAI/Azure 可把 Claude 入口改写到 Chat Completions URL：`relay/channel/openai/adaptor.go:120-123`、`relay/channel/openai/adaptor.go:166-170`。
5. 非流式响应由 `OpenaiHandler` 转入 `ResponseOpenAI2Claude`：`relay/channel/openai/relay-openai.go:189-291`。
6. 流式响应由 `OaiStreamHandler`、`HandleStreamFormat`、`StreamResponseOpenAI2Claude` 处理：`relay/channel/openai/relay-openai.go:103-186`、`relay/channel/openai/helper.go:20-47`。

### 2.2 必须修正的现状

1. 请求转换开始时，现有 `FinalRequestRelayFormat` 和 `RequestConversionChain` 尚不能可靠表达最终上游协议；conversion chain 是在 adaptor 转换返回后才追加。深度转换不能依赖该状态反推目标。
2. `ClaudeToOpenAIRequest` 被 Responses 桥接、Ali 兼容模式等路径共享，不能无条件加入 Chat 专属行为。
3. 当前流式终止事件分散在 `StreamResponseOpenAI2Claude` 和 `HandleFinalResponse`，不存在满足本任务语义的单一 finalizer。
4. 当前流式工具状态不足以缓存乱序到达的 ID、name 和 arguments。
5. 当前 `OpenaiHandler` 首次 JSON 解析失败即返回错误，不支持非流式请求收到 SSE body 的情况。

## 3. 范围

### 3.1 纳入范围

仅当客户端请求格式为 Anthropic Messages，且代码在转换前已明确最终上游协议为 OpenAI Chat Completions 时启用深度转换，包括：

- OpenAI Chat Completions 渠道；
- Azure Chat Completions；
- OpenRouter Chat Completions；
- Ali 非原生、实际发送 Chat Completions 的兼容模式；
- Advanced Custom 明确配置 `anthropic_messages_to_openai_chat_completions` converter 的路径。

### 3.2 排除范围

以下路径不得启用本任务的深度规则，行为必须保持不变：

- Anthropic Messages → OpenAI Responses；
- Anthropic Messages → Gemini；
- 原生 Anthropic Messages adaptor；
- DeepSeek、Moonshot 等原生 Messages 路径；
- Ali 原生 Anthropic Messages 模式；
- Advanced Custom 未明确配置上述 converter 的路径；
- pass-through request body；
- 其他协议入口或其他转换方向；
- 渠道级或模型级功能开关；
- 通用中间表示、通用转换框架或新协议层；
- cache token 计费精度重构。

## 4. R0：目标协议隔离

### 4.1 显式目标契约

必须增加一个在调用 `ClaudeToOpenAIRequest` **之前**即可确定的转换目标/options 契约，例如：

```go
type ClaudeConversionTarget int

const (
    ClaudeConversionTargetBase ClaudeConversionTarget = iota
    ClaudeConversionTargetOpenAIChatCompletions
)
```

具体命名可按现有代码风格调整，但必须满足：

1. 目标由调用点显式传入，不通过模型名、厂商名、URL 字符串或 conversion chain 猜测。
2. 现有基础行为保留为默认目标；只有 `OpenAIChatCompletions` 目标启用本 PRD 的深度规则。
3. 为避免接口扩散，可增加最小包装函数，例如基础转换与 Chat 深度转换分别调用同一内部实现；不得创建通用协议转换层。
4. 必须在 `RelayInfo` 或同等请求级状态中保存“本次请求已明确启用 Messages → Chat 深度转换”的标记，供非流式响应、流式响应和伪 SSE 兜底共同判定。
5. 该标记必须由明确转换调用设置，不能仅依据 `RelayFormatClaude` 或 `FinalRequestRelayFormat` 推断。

### 4.2 调用点要求

- OpenAI/Azure/OpenRouter 的 Claude → Chat adaptor：显式使用 Chat 深度目标。
- Ali：仅非原生且最终 URL 为 Chat Completions 的分支使用 Chat 深度目标；原生分支不变。
- Advanced Custom：仅 converter 精确为 `anthropic_messages_to_openai_chat_completions` 时使用 Chat 深度目标。
- Chat Completions via Responses：继续使用基础转换，不启用 Chat 深度目标。
- Claude → Gemini 等中间转换：继续使用基础转换。
- pass-through：不调用深度转换，也不设置深度标记。

实施前必须枚举当前仓库中 `ClaudeToOpenAIRequest` 的全部调用点，并为每个调用点增加隔离回归测试。

## 5. R1：请求深度转换

### 5.1 顶层字段

| Anthropic 字段 | Chat Completions 字段 | 规则 |
|---|---|---|
| `model` | `model` | 使用映射后的上游模型名 |
| `max_tokens` | `max_tokens` | 转换层保留；o-series/GPT-5 的 `max_completion_tokens` 规范化继续由 OpenAI adaptor 处理 |
| `temperature` | `temperature` | 透传，模型限制由目标 adaptor 处理 |
| `top_p` | `top_p` | 透传，模型限制由目标 adaptor 处理 |
| `top_k` | `top_k` | 保持项目现有兼容行为，不新增模型推断 |
| `stop_sequences` | `stop` | 单值输出 string，多值输出 array，空值不输出 |
| `stream` | `stream` | 透传 |
| `tool_choice.auto` | `tool_choice: "auto"` | 仅有可用工具时输出 |
| `tool_choice.any` | `tool_choice: "required"` | 仅有可用工具时输出 |
| `tool_choice.none` | `tool_choice: "none"` | 仅有可用工具时输出 |
| 指定 tool | OpenAI function selector | `{"type":"function","function":{"name":"..."}}` |
| `disable_parallel_tool_use` | `parallel_tool_calls` | 字段显式存在时取反映射 |
| `thinking` / `output_config.effort` | adaptor 已有 reasoning 配置 | 复用现有模型检测和 adaptor 逻辑，不新增通用模型前缀表 |

`metadata`、`service_tier`、`mcp_servers`、`context_management`、`container` 等 Anthropic 专有字段不得进入最终 Chat 请求。

`stream_options.include_usage` 继续由目标 adaptor 根据 `SupportStreamOptions` 注入。

### 5.2 system

1. string 转换为一条 system message。
2. array 只提取非空 text block。
3. 非 OpenRouter Claude 兼容路径使用 `"\n"` 连接多个 text block，不得无分隔拼接。
4. OpenRouter 路径保留当前 media content 与 `cache_control` 兼容能力，但同样跳过无效或空 block。
5. system/developer role 的模型适配继续由 OpenAI adaptor 处理。
6. system 最终为空时不发送空消息。

### 5.3 tools 与 JSON Schema

1. `tools[].input_schema` 转为 `tools[].function.parameters`。
2. 递归删除值严格等于 `"uri"` 的 `format` 字段。
3. 递归至少覆盖任意嵌套 object/array，因此自然覆盖 `properties`、`items`、组合 schema 和 `$defs` 内部结构。
4. 保留 `$schema`、`$defs`、`additionalProperties`、`patternProperties` 及其他 schema 关键字，不做白名单清洗。
5. tools JSON 解析失败、工具 name 缺失、schema 不是可转换对象或序列化失败时返回明确请求转换错误。
6. 单个无效工具不得导致静默丢弃全部工具。
7. 转换后没有可用工具时，不输出 `tools`、`tool_choice` 或 `parallel_tool_calls`。

### 5.4 messages

1. string content 原样转换。
2. content array 中 text、image、thinking、redacted_thinking、tool_use 可共存。
3. assistant 同时含 text/image 与 tool_use 时，必须同时保留 media content 和 `tool_calls`。
4. `tool_use` 转换为 `assistant.tool_calls[]`：
   - 保留 `id`；
   - `name` 转为 `function.name`；
   - `input` 使用项目 JSON 工具稳定序列化为 `function.arguments`；
   - 序列化失败返回请求转换错误。
5. `tool_result` 拆成独立 `role:"tool"` 消息：
   - `tool_call_id=tool_use_id`；
   - string content 原样使用；
   - array/object content 序列化为 JSON string；
   - 序列化失败返回请求转换错误。
6. image：
   - `source.type=base64` 转为 `data:{media_type};base64,{data}`；
   - `source.type=url` 直接使用 URL；
   - source nil、media type/data/URL 为空或 source type 未知时返回请求转换错误，禁止 panic。
7. assistant thinking 历史块：
   - 只有目标 adaptor 现有能力明确要求非标准 reasoning 字段时才写入 `reasoning_content`；
   - 严格 OpenAI Chat 路径不发送非标准 reasoning 字段；
   - `redacted_thinking` 使用固定、可测试的降级文本，不泄漏不存在的原文。
8. 转换后为空的普通消息不发送；带 tool calls 的 assistant message 允许 `content:null`。
9. 保持消息顺序；一个 Anthropic message 因 tool_result 拆分为多条 Chat message 时，拆分结果必须位于原消息对应位置。

## 6. R2：非流式响应深度转换

以下规则只在请求级深度标记为真时启用；其他 `OpenaiHandler` 调用者维持当前行为。

### 6.1 结构校验

1. 完整读取 body 后解析 `OpenAITextResponse`。
2. `choices` 为空时返回 `bad response`，不得生成成功 Anthropic 响应。
3. 只转换 `choices[0]`，不得拼接多个 choice。
4. DTO 必须补充接收 message-level `refusal`、content part refusal 以及 legacy `function_call` 所需字段。

### 6.2 content blocks

输出顺序固定为：

1. thinking；
2. text/refusal；
3. tool_use。

映射规则：

- `message.reasoning_content` 或 `message.reasoning` → `{type:"thinking",thinking:"..."}`；
- string content → text block；
- array content 中 `text`、`output_text` → text block；
- message-level refusal 和 content part refusal → text block；
- `tool_calls[]` 按上游顺序 → tool_use blocks；
- legacy `function_call` → 单个 tool_use，且与 `finish_reason:"function_call"` 成套支持。

工具 arguments：

- 合法 JSON object → `input`；
- 空字符串、非法 JSON 或合法但非 object → `{}`，并记录 warn；
- 不因单个 arguments 非法导致整条已完成响应失败。

### 6.3 finish reason

| OpenAI | Anthropic |
|---|---|
| `stop` | `end_turn` |
| `length` / `max_tokens` | `max_tokens` |
| `tool_calls` / `function_call` | `tool_use` |
| `content_filter` | 项目现有 `refusal` |
| 空或未知值 | `end_turn`，未知值记录 warn |

### 6.4 usage

1. usage 缺失时返回非 nil 的零值 usage。
2. cache read 与 cache creation 不得和普通 input token 重叠计算。
3. Anthropic `input_tokens` 采用：

```text
max(prompt_tokens - cache_read_input_tokens - cache_creation_input_tokens, 0)
```

4. cache 字段缺失视为 0；任何结果均不得为负数。
5. 本任务只保证协议字段一致，不改变计费侧 token 精度或现有计费流程。

## 7. R3：流式响应状态机

以下改造只对请求级深度标记为真的 Claude 输出启用，不得改变 OpenAI、Gemini 或基础 Claude 转换路径。

### 7.1 状态模型

扩展请求级 Claude stream state，至少保存：

- message 是否已开始；
- 当前打开的 content block 类型及稠密 content index；
- thinking/text block 生命周期；
- upstream tool index → Anthropic 稠密 content index 映射；
- 每个工具的 ID、name、pending arguments、是否已 start、是否已 stop；
- 首个非空 finish reason；
- 最后一个非 nil usage；
- terminal 是否已发送；
- stream error 是否已发送；
- 流是否失败。

状态必须以单请求隔离，不得使用包级共享状态。

### 7.2 converter 与 finalizer 的职责

必须形成明确的两个入口，具体函数名可调整：

```text
ConvertOpenAIChunkToClaudeBlocks(chunk)
FinalizeOpenAIToClaudeStream(reason, usage, eof)
```

职责约束：

1. chunk converter 只产生 message_start 和 block-level events，不产生 `message_delta`、`message_stop`。
2. 必须从 `StreamResponseOpenAI2Claude` 当前所有分支中移除 terminal event 发送逻辑，包括 usage-only 分支。
3. finalizer 是成功流发送 `message_delta` 和 `message_stop` 的唯一位置。
4. finalizer 幂等；每条成功流最多各发送一次 `message_delta` 和 `message_stop`。
5. first non-empty finish reason wins，后续 finish reason 不覆盖。
6. last non-nil usage wins。
7. finalizer 关闭尚未关闭的有效 block，但不得重复发送 `content_block_stop`。

### 7.3 chunk、最后 chunk 与 EOF

1. `OaiStreamHandler` 的延迟一条 chunk 机制必须在 scanner 结束后把最后一条尚未交给 converter 的 data chunk交付一次。
2. `[DONE]` 仅作为完成信号，不作为 JSON chunk 转换。
3. usage-only chunk 只更新 usage，不关闭 block、不发送 terminal events。
4. 收到 finish reason 后可以记录完成意图，但 terminal events 仍由统一 finalizer 发送。
5. 正常 EOF 且无 finish reason时：
   - 关闭仍打开的 block；
   - stop reason 使用 `end_turn`；
   - 使用最后一个非 nil usage；
   - 发送一次 terminal events。
6. scanner/read error 不按正常 EOF 成功收尾。

### 7.4 thinking、text 与工具调用

1. thinking 与 text 类型切换前必须关闭旧 block。
2. 处理每个 chunk 中全部 `delta.tool_calls[]`，包括第一条 chunk，不得只读第一个。
3. upstream tool index 必须映射为稠密、连续且不与 thinking/text block 冲突的 Anthropic content index。
4. arguments 早于 ID/name 到达时缓存；ID 和 name 足够后发送 start，再按原顺序 flush arguments delta。
5. 同一工具的 arguments fragment 不得串到其他工具。
6. finalizer 对 pending tool：
   - name 完整时补发 start；
   - ID 缺失时按固定规则生成稳定 placeholder，例如基于 upstream index；
   - name 缺失时丢弃该无效工具并记录 warn；
   - 已 start 的工具必须恰好 stop 一次。
7. placeholder 规则必须确定、可复现，并有精确单测。

### 7.5 流内错误

1. 在普通 `data:` payload 进入 Chat chunk DTO 前检测 `{"error":...}`。
2. 将上游错误转换为一次 Anthropic `event:error`。
3. 设置失败状态并立即停止 scanner 后续读取/转换。
4. 错误事件最多发送一次。
5. 失败流不得运行成功 finalizer，不得补发 `message_delta` 或 `message_stop`。
6. 已发送的 block events无需回滚，但错误后不得再发送任何成功事件。

### 7.6 UTF-8

保留现有按行 scanner 设计，先增加多字节字符跨底层 read 边界的测试。只有测试证明现有 scanner 会破坏 UTF-8 时，才增加最小缓冲修复。

## 8. R4：非流式请求收到 SSE 的兜底

### 8.1 启用条件

仅同时满足以下条件时启用：

- `OpenaiHandler` 已完整读取 body；
- 首次标准 JSON 解析失败；
- `RelayFormatClaude`；
- 请求级 Messages → Chat 深度标记为真；
- body 可被识别为 Chat Completions SSE。

不得把该逻辑放入 `handleClaudeFormat`，因为 `stream:false` 请求不会进入流式 handler。

### 8.2 Chat SSE accumulator

在 OpenAI channel 包内增加仅服务于 Chat Completions chunk 的聚合器；不得复用或修改 Responses 专用聚合语义。

聚合内容：

- `choices[0].delta.content`；
- reasoning/reasoning_content；
- refusal；
- 全部 tool calls，按 upstream index 分别累积 ID、name、arguments；
- 首个非空 finish reason；
- 最后一个非 nil usage。

完成判定：

1. `[DONE]` 视为明确完成；或
2. 已收到非空 finish reason，随后输入结束；
3. 单纯 EOF 且既无 `[DONE]` 又无 finish reason，视为截断响应并返回 bad response；
4. 无 choice 内容时返回 bad response；usage-only 不算 choice 内容；
5. SSE 中出现 `{"error":...}` 时返回上游错误，不得聚合为成功响应。

工具 arguments fragment 按字符串原序拼接，之后交给非流式响应转换器按 R2 解析。

聚合成功后构造单个 `OpenAITextResponse`，继续进入同一个深度版 `ResponseOpenAI2Claude`，避免形成第二套 Anthropic 响应映射。

响应继续使用当前 handler 的 HTTP 状态和输出方式；不得向客户端保留 `text/event-stream` 响应格式。

## 9. pass-through 契约

pass-through 的“行为不变”必须同时覆盖：

1. 原始 request body 不经过本任务转换；
2. 不设置深度转换标记；
3. 不启用深度版响应转换或伪 SSE 兜底；
4. 不因本任务新增 URL 改写；
5. 保持当前已有 URL、header、response 处理行为，不借本任务修复或改变既有 pass-through 语义。

## 10. 生效矩阵

| 实际路径 | 深度转换 |
|---|---|
| `/v1/messages` → OpenAI Chat Completions | 启用 |
| `/v1/messages` → Azure Chat Completions | 启用 |
| `/v1/messages` → OpenRouter Chat Completions | 启用 |
| `/v1/messages` → Ali 兼容 Chat Completions | 启用 |
| Advanced Custom 显式 Messages → Chat converter | 启用 |
| Chat Completions via Responses | 禁用 |
| Claude → Gemini | 禁用 |
| 原生 Anthropic Messages | 禁用 |
| DeepSeek/Moonshot 原生 Messages | 禁用 |
| Ali 原生 Messages | 禁用 |
| Advanced Custom 未指定对应 converter | 禁用 |
| pass-through | 禁用 |

矩阵必须由代码级显式目标和请求级标记落实，不得只靠文档、模型名或渠道名约定。

## 11. 测试矩阵

### 11.1 目标隔离与调用点

- 枚举并覆盖全部 `ClaudeToOpenAIRequest` 调用点；
- OpenAI、Azure、OpenRouter Chat 启用；
- Ali Chat 启用、Ali native 禁用；
- Advanced Custom 指定 converter 启用，未指定禁用；
- Responses bridge 零变化；
- Gemini 中间转换零变化；
- 原生 Anthropic、DeepSeek、Moonshot 零变化；
- pass-through body、URL 和响应行为零变化。

### 11.2 请求转换

- system string、array、空 block、换行连接、OpenRouter block 过滤；
- tool_choice auto/any/none/指定 tool；
- `disable_parallel_tool_use` 显式 true/false/缺失；
- schema 深层 object/array、properties/items/$defs 及仅删除 `format:"uri"`；
- tools 非法 JSON、缺 name、非法 schema 返回错误；
- text + image + tool_use 混合 assistant message；
- base64、URL、nil source、空字段、未知 source type；
- tool_use + tool_result 多轮及消息顺序；
- tool input/tool_result 序列化失败；
- thinking/redacted_thinking 保留与丢弃；
- 无工具时不输出悬空 tool_choice/parallel_tool_calls；
- o-series/GPT-5 继续复用 adaptor 的 max token 规范化；
- 最终发送 body 不含 Anthropic 专有字段。

### 11.3 非流式响应

- thinking + text + 多 tool calls 固定顺序；
- string content 与 array content；
- message-level refusal 与 content part refusal；
- legacy function_call；
- 只转换 choices[0]；
- 空 choices 返回 bad response；
- 合法 object、非法、空、非 object tool arguments；
- finish reason 全映射与未知值降级；
- usage 缺失返回零值；
- cache read/cache creation 扣减、缺失及大于 prompt token 时 clamp 到 0。

### 11.4 流式响应

- 纯文本完整事件序列；
- thinking → text、text → thinking 切换；
- text → 多 tool calls；
- 第一 chunk 同时含多个 tool calls；
- arguments 早于 ID/name；
- 多工具 arguments 交错；
- upstream 稀疏 index → Anthropic 稠密 index；
- missing ID 稳定 placeholder；
- missing name 丢弃并 warn；
- first finish reason wins；
- finish 后 usage-only chunk，last usage wins；
- 无 finish reason正常 EOF；
- 最后 data chunk 恰好转换一次；
- block 已关闭时 finalizer 不重复 stop；
- finalizer 重复调用不重复 terminal events；
- 流内错误只发一次；
- 错误后无成功 terminal events；
- scanner/read error 不成功收尾；
- UTF-8 多字节字符跨底层 read 边界。

### 11.5 非流式伪 SSE

- 标准 `stream:false` JSON 响应；
- content/reasoning/tool calls 聚合；
- `[DONE]` 完成；
- finish reason + EOF 完成；
- usage-only 尾 chunk；
- SSE error 返回上游错误；
- 无 choice；
- 只有 EOF、无 finish reason、无 `[DONE]` 的截断响应；
- 非 Claude或未启用深度标记时不触发兜底。

## 12. 实施边界

1. 优先修改现有 `service.ClaudeToOpenAIRequest`、`ResponseOpenAI2Claude`、`StreamResponseOpenAI2Claude` 及 OpenAI adaptor/handler 周边最小代码。
2. 允许增加目标 options、最小包装函数、请求级 stream state、Chat SSE accumulator 和必要 DTO 字段。
3. 不允许新建通用协议转换层或通用 IR。
4. 所有深度响应行为必须受请求级深度标记保护，禁止全局改变 `OpenaiHandler` 或 `HandleStreamFormat` 的其他格式行为。
5. 不通过扩大模型前缀表解决 reasoning 或 token 参数兼容；复用 OpenAI adaptor 现有规则。
6. 不修改本任务无关渠道的请求或响应结构。

## 13. 验收标准

1. Claude Code 或严格 Anthropic Messages 客户端经 `/v1/messages` 调用明确的 Chat Completions 上游，可完成文本、thinking、图片、工具调用和 tool_result 多轮交互。
2. 最终上游 URL 为对应渠道的 Chat Completions URL，最终 body 符合 OpenAI Chat Completions 结构且不含 Anthropic 专有字段。
3. 非流式响应符合 Anthropic Messages JSON，空/错误上游结构不会伪装为成功。
4. 流式 block 生命周期合法；成功流 `message_delta`、`message_stop` 各最多一次；失败流不发送成功 terminal events。
5. 最后一条 chunk、usage-only chunk、无 finish reason EOF、并行工具和乱序工具增量均正确处理。
6. 非流式请求收到完整 Chat SSE 时可聚合；错误或截断 SSE 返回明确错误。
7. Responses、Gemini、原生 Messages、Ali native、Advanced Custom 非目标 converter 和 pass-through 路径行为零变化。
8. R0-R4 正向、负向和共享调用点回归测试齐全。
9. 受影响 Go 包测试通过。
10. `go build ./...` 通过。

## 14. 原 PRD 审核结论

原 `PRD-messages-to-chat-completions-deep-convert.md` 的总体方向正确，但不能直接作为无歧义实施依据，原因包括：

1. 未定义转换执行前即可使用的显式目标协议契约；
2. 生效矩阵遗漏 Ali 兼容 Chat 路径；
3. 将当前不存在的统一 finalizer 当作既有边界；
4. 未明确 terminal event 必须从 chunk converter 中移除；
5. 未定义每个流式工具所需的 pending 状态；
6. cache token 去重缺少精确公式；
7. 伪 SSE 聚合的完成、截断及工具拼接语义不完整；
8. pass-through 的 URL 与响应侧“不变”边界不够明确。

本 PRD 补齐上述约束后，可在 new-api 现有 Claude → OpenAI 骨架内实施，并保持任务范围严格限定为 Anthropic Messages → OpenAI Chat Completions。
