# PRD：Anthropic Messages → OpenAI Chat Completions 深度转换（源码校正版）

> 文档 ID：`20260809T171206-a82b1611-8425-44c5-a724-981d4aa06fd1`
> 目标分支：`codex/deep-convert-messages2chat`
> 代码基线：当前分支 `24d18be8`
> 判断依据：new-api 当前源码；未使用 `.trellis/` 下任何文档

## 1. 结论

现有 `PRD-20260809T165527-27135c3a-2ab4-45c2-b0b2-dee61e468431.md` 方向正确，但不能直接作为无歧义实施依据，主要原因是：

1. 生效矩阵遗漏多个实际 Chat Completions 路径，并未区分 `ClaudeToOpenAIRequest` 的直接调用与间接 adaptor 委托。
2. 新增转换标记没有定义跨渠道重试时的重置规则；当前 `RelayInfo` 会跨 retry attempt 复用，存在状态泄漏风险。
3. `disable_parallel_tool_use` 要求区分缺失与显式 `false`，但当前 DTO 的 `bool` 无法表达该差异。
4. 流式同一 chunk 内 reasoning、text/refusal、tool calls 共存时的处理顺序未定义。
5. 工具 `index=nil`、ID/name 冲突、缺失字段及 pending 状态的处理规则不完整。
6. 非流式请求收到 SSE 的实际分流依据是响应 `Content-Type`；不能只在 `OpenaiHandler` JSON 解析失败后处理。
7. 伪 SSE 聚合后的响应头清理、错误状态、截断判定不完整。
8. cache token 扣减公式会改变当前 new-api usage 语义，且没有源码依据证明所有上游的 `prompt_tokens` 均包含 cache token，因此不应纳入本任务。
9. 基于 tool index 伪造缺失 tool ID 会掩盖损坏的上游响应，且没有现有源码契约支持。

本 PRD 替代上述文档，目标保持不变：

```text
客户端：POST /v1/messages（Anthropic Messages）
                    ↓
new-api：现有 Claude → OpenAI 转换骨架上的深度转换
                    ↓
上游：OpenAI Chat Completions wire protocol
```

不新建通用协议转换层，不引入通用 IR，不扩展到其他转换方向。

## 2. 源码基线

### 2.1 已有骨架

- Claude 请求入口及 adaptor 分派：`relay/claude_handler.go:134-165`。
- 基础请求转换：`service/convert.go:18-218`。
- OpenAI adaptor 转换与 Chat URL 改写：`relay/channel/openai/adaptor.go:52-81`、`relay/channel/openai/adaptor.go:109-171`。
- 非流式 OpenAI → Claude：`relay/channel/openai/relay-openai.go:188-290`、`service/convert.go:635-674`。
- 流式转换：`relay/channel/openai/relay-openai.go:103-185`、`relay/channel/openai/helper.go:20-47`、`service/convert.go:321-628`。
- 请求级 Claude stream state：`relay/common/relay_info.go:36-51`。

### 2.2 `ClaudeToOpenAIRequest` 的直接调用点

当前直接调用点只有三个：

1. OpenAI adaptor：`relay/channel/openai/adaptor.go:63`。
2. Chat Completions via Responses bridge：`relay/claude_handler.go:138`。
3. Ali 非原生兼容分支：`relay/channel/ali/adaptor.go:77`。

Advanced Custom、Gemini、Ollama、Volcengine、Perplexity 等通过 adaptor 委托间接到达 OpenAI adaptor，不能只按三个直接调用点决定是否启用深度转换。

### 2.3 当前已有、必须保留的能力

以下不是从零开发，改造时必须保留：

- model、temperature、max tokens、top_p、top_k、stream、stop sequence 的基础映射；
- tools 的基础 function 映射；
- system string 与 OpenRouter system media/cache-control 兼容；
- text、base64 image、tool_use、tool_result 的基础转换；
- OpenAI adaptor 对 o-series/GPT-5 token 参数的既有规范化；
- stream upstream tool index → Anthropic 稠密 index 映射和 started block 跟踪：`relay/common/relay_info.go:36-51`、`service/convert.go:261-320`。

## 3. 范围

### 3.1 纳入范围

仅处理：

```text
入口协议 = Anthropic Messages
最终上游 wire protocol = OpenAI Chat Completions
```

“最终”指请求实际发送到 Chat Completions 兼容端点，而不是仅在中间阶段生成 `GeneralOpenAIRequest`。

### 3.2 排除范围

- Messages → OpenAI Responses；
- Messages → Gemini；
- Messages → Ollama `/api/chat`；
- 原生 Anthropic/Claude Messages；
- DeepSeek、Moonshot、MiniMax、Ali native、Volcengine special-base 等原生 Messages 路径；
- AWS/Bedrock Claude 路径、Vertex Claude 路径；
- Advanced Custom 未配置目标 converter；
- pass-through body；
- Baidu V2、SiliconFlow 等当前 endpoint/body 路由不一致问题；本任务不得顺带修复或改变其既有行为；
- 其他入口协议及其他转换方向；
- 通用协议层、通用 IR；
- 渠道或模型功能开关；
- token 计费精度和 cache token 语义重构。

## 4. R0：按 attempt 隔离的最终 wire target

### 4.1 显式 target

增加最小、专用的转换目标契约，命名可按项目风格调整：

```go
type ClaudeConversionTarget int

const (
    ClaudeConversionTargetBase ClaudeConversionTarget = iota
    ClaudeConversionTargetOpenAIChatCompletions
)
```

必须满足：

1. target 由调用路径显式选择，不通过模型名、厂商名、URL 字符串、`RelayFormatClaude`、`FinalRequestRelayFormat` 或 conversion chain 猜测。
2. `ClaudeToOpenAIRequest` 保持基础行为；增加最小 wrapper/options 进入 Chat 深度逻辑，不创建通用转换框架。
3. OpenAI adaptor 被其他 adaptor 当中间转换器使用时，调用者必须显式传入 Base 或 Chat target。
4. Chat target 成功进入转换时，在 `RelayInfo` 中记录本次 attempt 已启用深度转换，供请求发送、非流式响应、流式响应和 SSE 聚合共同使用。
5. response 侧只能读取该显式标记，不得自行推断。

### 4.2 retry 生命周期

当前 `RelayInfo` 在 controller retry loop 外创建并跨 attempt 复用：`controller/relay.go:121-231`。因此必须：

1. 每个新 channel attempt 开始时，将 Claude conversion target、深度标记、深度 stream state、terminal/error 状态重置为 Base/零值。
2. 只有当前 attempt 的明确 Chat target 转换可重新设置深度标记。
3. attempt A 的 Chat 标记不得泄漏到 attempt B 的 Responses、Gemini、Ollama 或 native Messages 路径。
4. 已向客户端发送响应事件后不得再进行会产生第二套协议状态的渠道重试。
5. conversion chain 继续用于记录，不作为 target 判定依据。

### 4.3 调用路径分类

#### 启用 Chat target

- OpenAI adaptor 最终发送 `/v1/chat/completions`；
- Azure Chat Completions；
- OpenRouter Chat Completions；
- Xinference/旧式 Custom 等直接使用 OpenAI adaptor且最终为 Chat Completions 的路径；
- Ali non-native `/compatible-mode/v1/chat/completions`；
- Advanced Custom route 精确配置 `anthropic_messages_to_openai_chat_completions`；
- Perplexity `/chat/completions`；
- Volcengine 非 special-base Chat Completions；
- 其他经源码枚举确认“最终 URL、wire DTO、response handler 均为 Chat Completions”的现有路径。

#### 使用 Base target或不调用转换

- Chat Completions via Responses bridge；
- Gemini 中间转换；
- Ollama `/api/chat` 中间转换；
- Anthropic、DeepSeek、Moonshot、MiniMax、Ali native；
- AWS/Bedrock Claude、Vertex Claude；
- Volcengine special-base native Messages；
- Advanced Custom converter `none`；
- pass-through；
- 当前最终 endpoint 不是 Chat Completions 或不能由源码确认的路径。

### 4.4 pass-through

- 原始 body 不进入本任务转换；
- 不设置深度标记；
- 不启用深度响应转换或 SSE 聚合；
- 不新增 URL 改写；
- Advanced Custom converter `none` 保持现有 native pass-through；
- Advanced Custom 非 `none` converter + pass-through 保持当前明确报错：`relay/channel/advancedcustom/adaptor.go:192-198`。

## 5. R1：请求深度转换

### 5.1 顶层字段

| Anthropic | Chat Completions | 规则 |
|---|---|---|
| `model` | `model` | 使用现有模型映射后的上游模型 |
| `max_tokens` | `max_tokens` | 转换层保留；后续 adaptor 继续执行既有 token 参数规范化 |
| `temperature` | `temperature` | 透传 |
| `top_p` | `top_p` | 透传 |
| `top_k` | `top_k` | 保持当前兼容行为 |
| `stop_sequences` | `stop` | 1 个为 string；多个为 array；空则省略 |
| `stream` | `stream` | 透传，同时在 RelayInfo 保存原始客户端 stream 意图 |
| `tool_choice.auto` | `"auto"` | 仅存在有效 tools 时发送 |
| `tool_choice.any` | `"required"` | 仅存在有效 tools 时发送 |
| `tool_choice.none` | `"none"` | 作为兼容扩展；仅存在有效 tools 时发送 |
| 指定 tool | function selector | `{"type":"function","function":{"name":"..."}}` |
| `disable_parallel_tool_use` | `parallel_tool_calls` | 字段存在时取反；缺失时省略 |

必须调整 DTO 或解析方式，使 `disable_parallel_tool_use` 能区分：

- 缺失；
- 显式 `false`；
- 显式 `true`。

可使用 `*bool`、presence flag 或保留原始对象字段存在性；不得继续用无法区分缺失与 false 的普通 `bool` 实现三态语义。

Anthropic 输入中的 `metadata`、`service_tier`、`mcp_servers`、`context_management`、`container` 不由该转换器复制。渠道参数覆盖若按现有机制增加 OpenAI 字段，不属于“Anthropic 字段泄漏”，不得被本任务全局禁止。

`thinking`、`output_config.effort` 仅保留源码已有、目标 adaptor 已明确支持的行为：

- OpenRouter 继续使用现有 `reasoning` 和 `verbosity` 映射；
- 非 OpenRouter 继续保留现有 `-thinking` suffix 行为；
- 不新增模型前缀表，不把 `verbosity` 错写为 `reasoning_effort`，不向严格 OpenAI 路径凭空发送非标准字段。

### 5.2 system

1. string 非空时生成一条 system message。
2. array 只接受 `type=text` 且 text 非空的 block；空定义为 `text == ""`，不擅自 trim 用户内容。
3. 非 OpenRouter Claude 路径用 `"\n"` 连接有效 block。
4. OpenRouter Claude 路径保留有效 text block 的 media content 与 `cache_control`。
5. 最终无有效文本时不发送 system message。
6. system/developer role 模型适配继续由 OpenAI adaptor 处理。

### 5.3 tools 与 schema

1. 将 `ClaudeRequest.Tools` 转换为支持的 tool DTO；形状转换失败必须返回明确请求转换错误，禁止忽略 `Any2Type` 错误。
2. 每个 tool 必须有非空 name，`input_schema` 必须能转换为 JSON object。
3. `input_schema` 映射为 `function.parameters`。
4. 递归遍历任意 object/array，仅删除值严格等于字符串 `"uri"` 的 `format` 字段。
5. 保留 `$schema`、`$defs`、`additionalProperties`、`patternProperties`、组合 schema 及其他关键字。
6. 任意单个 tool 无效时返回转换错误，不静默丢弃该 tool 或全部 tools。
7. 转换后无有效 tools 时不发送 `tools`、`tool_choice`、`parallel_tool_calls`。

### 5.4 messages

1. string content 原样转换。
2. array 中 text、image、thinking、redacted_thinking、tool_use、tool_result 可共存。
3. assistant 同时含 text/image 与 tool_use 时，同时保留 content 和 `tool_calls`；修复当前 `len(toolCalls)==0` 才设置 media content 的限制。
4. `tool_use`：
   - ID、name 必须非空；
   - `input` 使用 `common.Marshal` 序列化为 arguments；
   - 序列化失败返回请求转换错误；
   - 不使用吞错后返回 `{}` 的 helper。
5. `tool_result` 拆为独立 `role:"tool"` message：
   - `tool_call_id=tool_use_id`，ID 为空时返回转换错误；
   - string 原样使用；
   - object/array 直接序列化原始 `mediaMsg.Content`，不得先强制转换为 `[]ClaudeMediaMessage`；
   - 序列化失败返回转换错误。
6. image：
   - source 不得为 nil；
   - `base64` 要求 `media_type` 非空且 `data` 为非空 string，输出 data URL；
   - `url` 要求 URL 非空并直接输出；
   - 未知 type 或字段无效返回转换错误，禁止 panic。
7. assistant 历史 thinking：
   - 仅目标 adaptor 的现有能力明确需要非标准 reasoning 字段时写入；
   - 严格 OpenAI Chat 不发送非标准历史 reasoning 字段；
   - `redacted_thinking` 使用固定文本：`[redacted thinking]`，不得伪造原文。
8. 空普通 message 不发送；含 tool calls 的 assistant message 允许 `content:null`。
9. 保持 Anthropic message 顺序。一个 message 内的普通 assistant content/tool calls 先作为 assistant message输出，随后按 block 顺序输出拆分的 tool result messages；不得像当前实现一样把 tool result 提前到其所属普通内容之前。

## 6. R2：非流式 Chat 响应 → Anthropic Messages

仅在当前 attempt 深度标记为真时启用；基础 converter 的其他调用者保持原行为。

### 6.1 结构与 DTO

1. 完整读取 body 并解析 `OpenAITextResponse`。
2. `choices` 为空返回 `types.ErrorCodeBadResponseBody`，HTTP 状态使用 `502 Bad Gateway`，允许在尚未向客户端写出响应时按现有 relay 策略重试。
3. 只处理 `choices[0]`。
4. DTO 增加：
   - message-level `refusal string`；
   - content part 的 `text`、`output_text`、`refusal`；
   - legacy response `function_call {name, arguments}`；
   - stream `delta.refusal`。
5. content part `type=refusal` 时优先读取 `refusal`，为空再读取 `text`。

### 6.2 content block 顺序

固定输出：

1. thinking；
2. text/output_text/refusal；
3. tool_use。

规则：

- `reasoning_content` 优先于 `reasoning`，两者不拼接；
- string content → text；
- array 中 `text`、`output_text` → text；
- message-level refusal 和 part refusal → text；
- `tool_calls[]` 保持上游数组顺序；
- legacy `function_call` 生成一个 tool_use。

工具 arguments：

- 合法 JSON object → `input`；
- 空、非法 JSON、null、array 或 scalar → `{}`；
- 降级不使整条已完成响应失败；
- warning 由 OpenAI handler 使用现有请求上下文记录，service converter 返回可识别的降级信息或接受 context，不使用无上下文全局猜测日志。

### 6.3 finish reason

| OpenAI | Anthropic |
|---|---|
| `stop` | `end_turn` |
| `length`、`max_tokens` | `max_tokens` |
| `tool_calls`、`function_call` | `tool_use` |
| `content_filter` | 项目现有 `refusal` |
| 空 | `end_turn` |
| 未知 | `end_turn`，handler 记录 warning |

深度映射不得全局修改共享 `reasonmap` 后影响其他协议方向；如修改共享 mapper，必须有兼容模式或专用入口。

### 6.4 usage

本任务保持当前 new-api usage 语义，不实施无源码依据的 cache token 扣减：

- `input_tokens = max(prompt_tokens, 0)`；
- `output_tokens = max(completion_tokens, 0)`；
- cache read、cache creation 继续作为独立字段按当前来源映射，并分别 clamp 到非负；
- usage 缺失时返回非 nil 零值 usage；
- cache creation aggregate 与 5m/1h split 的现有归一化行为保持不变。

## 7. R3：流式状态机

仅对当前 attempt 深度标记为真且客户端请求 `stream:true` 的路径启用。

### 7.1 状态

在现有 `ClaudeConvertInfo` 基础上增加或整理：

- message started；
- 当前 text/thinking block 及 started/stopped 状态；
- 保留现有 upstream tool index → 稠密 content index 能力；
- 每个 tool 的 upstream index、ID、name、pending arguments、started、stopped；
- first non-empty finish reason；
- last non-nil upstream usage；
- terminal delta sent、message stop sent；
- error event sent、failed；
- message ID/model fallback 所需字段。

只保留一个 finish reason 数据源，避免 `RelayInfo.FinishReason` 与嵌入 state 同名字段分裂。

状态按 attempt 隔离，数据 callback 串行更新；finalizer 只能在 scanner goroutine 完成并 join 后运行。

### 7.2 converter 与 finalizer

形成两个明确入口，命名可调整：

```text
ConvertOpenAIChunkToClaudeBlocks
FinalizeOpenAIToClaudeStream
```

- converter 只产生 `message_start` 和 block-level events。
- converter 不产生 `message_delta`、`message_stop`，也不修改 terminal sent 状态。
- 移除当前 first-finish、usage-only、ordinary-finish 分支及 `HandleFinalResponse` 中所有 terminal event/Done 写入。
- held last chunk 必须恰好一次进入普通 converter；之后只调用 finalizer，不得再次转换。
- finalizer 是 `message_delta`、`message_stop` 的唯一生产者和 terminal 状态唯一写入者。
- successful stream 总是各发送一次 `message_delta` 和 `message_stop`；usage 缺失时 delta 带非 nil 零值 usage。
- first non-empty finish reason wins；last non-nil upstream usage wins，估算 usage 不覆盖已收到的 upstream usage。
- finalizer 重复调用不重复发送已成功写出的事件。
- 若 `message_delta` 成功而 `message_stop` 写失败，状态只记录 delta 已发送；重复调用仅可尝试尚未成功发送的 stop。
- 写失败将 stream 标记为 failed，不得宣称成功完成。

### 7.3 message_start 与 choice

1. 所有 chunk 只消费 `choices[0]`，包括 delta、finish reason 和 tool calls。
2. 第一个有效 choice-zero chunk触发 `message_start`，且必须先于任何 block event。
3. usage-only、错误 envelope、无法解析的 chunk 不触发 message_start。
4. finish-only 的有效 choice 也必须先产生 message_start，随后由 finalizer结束。
5. message ID 缺失时使用 `msg_<upstream response id>`；response ID 也为空时使用当前 request ID 派生的稳定值。
6. model 缺失时使用 `info.UpstreamModelName`。
7. message_start 发出后，后续 ID/model 不反向修改已发送消息。

### 7.4 同 chunk 字段顺序与 block 生命周期

同一个 `choices[0].delta` 中字段可共存，固定按以下顺序处理，任何类型不得压掉其他类型：

1. reasoning；
2. content 与 refusal；
3. `tool_calls[]` 全部元素。

类型切换前关闭当前已开始且未停止的 text/thinking block。text/refusal 共用 text block。每个 started block 恰好 stop 一次。

### 7.5 tool 增量

1. 保留按首次到达顺序分配稠密 Anthropic content index 的现有能力；不得按稀疏 upstream index 直接输出。
2. `arguments` 按到达原序 append。
3. ID 和 name 使用 first non-empty wins；后续相同值忽略，冲突值保留第一个并记录 warning。
4. ID 与 name 都非空后发送 `content_block_start`，随后按缓存顺序 flush arguments。
5. arguments 不得跨 tool index 混合。
6. `tool_calls[].index=nil`：
   - 当前仅有一个已知/pending tool 时归属该 tool；
   - 否则该 chunk 为歧义上游数据，停止成功转换并发送一次 Anthropic stream error。
7. finalizer：
   - ID/name 完整但尚未 start：补发 start并 flush arguments；
   - ID 或 name 缺失：视为不完整上游响应，丢弃该未开始 tool 并记录 warning；不得伪造 tool ID；
   - 已 start 的 tool 必须 stop 一次；
   - 已 start 后出现身份冲突不重开 block。

### 7.6 finish、EOF 与 usage

- `[DONE]` 是完成信号，不进入 JSON converter。
- usage-only chunk 仅更新 last non-nil usage。
- finish reason只记录 first non-empty value，不直接终止事件。
- 正常 EOF无 finish reason时使用 `end_turn` 成功收尾。
- scanner error、timeout、client gone、panic、致命解析错误、上游 error envelope 均不是正常 EOF，不运行成功 finalizer。
- finalizer 不只依赖 `StreamStatus.IsNormalEnd()`；必须同时检查明确 end reason、errors 和 state.failed。

### 7.7 错误

1. 在 Chat chunk DTO 解析前检测任意非 null `error` 字段，不要求 `error.type` 存在。
2. 转换为一次 Anthropic：

```text
event: error
data: {"type":"error","error":{"type":"api_error","message":"..."}}
```

3. 消息使用项目已有错误清洗策略；上游 type 缺失时使用 `api_error`。
4. 使用 scanner stop 语义立即停止后续读取，不得仅记录 soft error 后继续。
5. failed stream 不发送成功 terminal events。
6. scanner/transport error 若客户端连接仍可写，也发送至多一次同结构 error；写不出时只记录失败。
7. 已写出任何 downstream stream event 后禁止 relay retry。

### 7.8 UTF-8 与 SSE 行

先增加多字节字符跨底层 read 边界测试。`bufio.Scanner` 按完整行返回通常不会因此破坏 UTF-8；测试未复现时不改 scanner 缓冲逻辑。

本任务不把共享 scanner 扩建为通用 RFC SSE 框架，只实现本方向所需的最小、明确数据行语义。

## 8. R4：客户端 `stream:false` 时的 Chat SSE 聚合

### 8.1 原始客户端意图

当前代码可根据上游 `Content-Type:text/event-stream` 把 `info.IsStream` 改为 true。为保证严格 Anthropic 客户端语义，必须单独保存原始客户端 stream 意图：

- 客户端 `stream:true`：走 R3 流式状态机；
- 客户端 `stream:false` 且当前 attempt 为深度 Chat target：无论上游 SSE 是否正确标注 Content-Type，都缓冲并聚合成单个 Anthropic JSON；
- 其他路径保持现有行为。

### 8.2 两种入口

1. 上游正确返回 `Content-Type:text/event-stream`：在 response handler 分流时进入 Chat SSE accumulator，不直接向 `stream:false` 客户端透传 SSE。
2. 上游 Content-Type 错误或缺失：`OpenaiHandler` 完整读 body，标准 JSON parse 失败且 body 满足下述 SSE 识别规则时进入同一个 accumulator。

两种入口必须复用同一聚合器和同一 R2 response converter。

### 8.3 最小 SSE 识别规则

- 仅提取以 `data:` 开头的行，允许冒号后有一个可选空格；
- 忽略空行、comment、`event:`、`id:`、`retry:`；
- 每条 data 行必须独立包含一个完整 Chat JSON chunk，或完整 `[DONE]`；
- 不支持 multiline data 拼接，遇到无法独立解析的 data payload 返回 bad response；
- 不能只因出现 `data:` 就判定为 Chat SSE，至少要成功解析一个 Chat chunk；
- `[DONE]` 只在提取 data value 后识别；
- `[DONE]` 后的数据忽略；重复 `[DONE]` 忽略；
- `[DONE]` 前任何 malformed JSON 均失败，不静默跳过。

### 8.4 聚合规则

只聚合 `choices[0]`：

- reasoning_content/reasoning；
- content；
- refusal；
- 全部 tool calls；
- first non-empty finish reason；
- last non-nil usage。

同 chunk 按 reasoning、content/refusal、tool calls 顺序。tool identity、nil index、冲突规则与 R3 一致。最终 tool calls 按首次到达顺序输出，arguments 原序拼接。

完成条件：

1. 收到 `[DONE]`；或
2. 收到 finish reason 后输入正常结束。

失败条件：

- EOF 且无 `[DONE]`、无 finish reason；
- 未成功解析任何 choice-zero chunk；
- 只有 usage chunk；
- malformed data；
- 非 null error envelope；
- 歧义 nil tool index。

finish-only choice 视为有效 choice，构造空 content 并由 R2 按 finish reason 转换。

### 8.5 错误与响应头

- buffered SSE 中任意非 null error 字段返回 `types.ErrorCodeBadResponseBody` 和 `502 Bad Gateway`，不得把外层 HTTP 200 当作成功。
- 聚合成功后构造一个 `OpenAITextResponse`，进入 R2 唯一 converter。
- 输出必须是正常 Anthropic JSON，不得保留 `text/event-stream`。
- 不复制上游 `Content-Length`、`Transfer-Encoding`、`Connection` 及 SSE 专用 cache/buffering headers。
- 设置项目正常 Claude JSON `Content-Type`，按最终 JSON 重算 Content-Length。
- 仅复制现有安全响应头；不得直接使用会批量复制上游 SSE headers 的输出路径而不做清理。

## 9. 生效矩阵

| 实际路径 | 深度转换 |
|---|---|
| OpenAI `/v1/chat/completions` | 启用 |
| Azure Chat Completions | 启用 |
| OpenRouter Chat Completions | 启用 |
| Xinference/Custom 最终 Chat Completions | 启用 |
| Ali compatible-mode Chat Completions | 启用 |
| Advanced Custom 精确 Messages→Chat converter | 启用 |
| Perplexity Chat Completions | 启用 |
| Volcengine 普通 Chat Completions | 启用 |
| Chat Completions via Responses | 禁用 |
| Gemini / Vertex Gemini mode | 禁用 |
| Ollama `/api/chat` | 禁用 |
| Anthropic/DeepSeek/Moonshot/MiniMax native Messages | 禁用 |
| Ali native Messages | 禁用 |
| Volcengine special-base native Messages | 禁用 |
| AWS/Bedrock Claude、Vertex Claude | 禁用 |
| Advanced Custom converter `none` | 禁用 |
| pass-through | 禁用 |
| Baidu V2/SiliconFlow 当前不一致路径 | 保持现状，本任务不修复 |

判定必须由代码级显式 target 落实，而不是渠道名字符串或文档约定。

## 10. 测试矩阵

### 10.1 target 与 retry 隔离

- 三个直接 `ClaudeToOpenAIRequest` 调用点；
- 所有间接委托 OpenAI adaptor 的 `ConvertClaudeRequest` 路径；
- 每条路径验证最终 URL、wire DTO、response handler 与 target；
- 正向矩阵全部启用；负向矩阵全部禁用；
- Chat attempt → native/Responses/Gemini retry 时标记清零；
- native/Responses/Gemini attempt → Chat retry 时仅后者启用；
- Advanced Custom 按 matched route 隔离；
- Ali env 配置覆盖、大小写、substring native 判定；
- pass-through body、URL、response 行为不变；Advanced Custom converter + pass-through 保持报错。

### 10.2 请求

- system string、array、空 text、非 text、换行、OpenRouter cache_control；
- stop 0/1/N；
- tool_choice auto/any/none/tool；
- disable_parallel_tool_use 缺失/false/true；
- tools shape 错误、name 为空、schema 非 object；
- 深层 properties/items/$defs/组合 schema；仅删除 `format:"uri"`；
- assistant text+image+多 tool_use；
- image base64/url/nil source/非 string data/空字段/未知 type；
- tool_use 序列化失败；
- tool_result string/object/array/序列化失败及顺序；
- thinking/redacted_thinking；
- 空 message、tool-only assistant content:null；
- o-series/GPT-5 adaptor 既有规范化不回归；
- Anthropic 专有输入字段不被 converter 复制。

### 10.3 非流式

- choices 为空返回 502 bad response body；
- 只处理 choices[0]；
- reasoning_content 优先于 reasoning；
- string content、text/output_text parts；
- message/part refusal precedence；
- thinking→text/refusal→tool_use 固定顺序；
- 多 tool_calls、legacy function_call；
- arguments object/空/非法/null/array/scalar；
- finish reason 全映射和未知降级；
- usage 缺失、负数 clamp；
- cache usage 保持当前独立字段语义；
- Responses/Gemini 共享 converter 的基础行为不变。

### 10.4 流式

- 纯文本完整事件序列；
- reasoning→text、text→reasoning；
- 同 chunk reasoning+text；
- 同 chunk reasoning/text+多个 tool calls；
- 只消费 choices[0]；
- usage-only 位于首 chunk、finish 前、finish 后；
- finish-only 首 chunk；
- message ID/model 缺失 fallback；
- arguments 早于 ID/name；
- 多工具 arguments 交错；
- 稀疏 index → 稠密 index；
- nil index：唯一 pending 成功、多 pending 报错；
- ID/name 重复与冲突 first-wins；
- missing ID/name 不伪造、不启动、finalizer 丢弃并 warn；
- first finish reason、last upstream usage；
- 正常 EOF无 finish reason；
- held last chunk只转换一次；
- finalizer 幂等；
- message_delta 成功、message_stop 写失败后的重试语义；
- error 无 type、malformed chunk、scanner error、timeout；
- error 后无成功 terminal；
- downstream 已写事件后不 retry；
- UTF-8 跨底层 read 边界。

### 10.5 `stream:false` + SSE

- 正确 `text/event-stream` 进入 accumulator 而非向客户端透传；
- 错误/缺失 Content-Type，通过 body sniff 进入同一 accumulator；
- content/reasoning/refusal/tools 聚合；
- `[DONE]`、重复 `[DONE]`、其后 data；
- finish + EOF；
- usage-only tail；
- error envelope有/无 type；
- malformed data；
- EOF 截断；
- 无 choice、只有 usage、finish-only choice；
- nil tool index与 identity 冲突；
- 输出 Content-Type、Content-Length、Transfer-Encoding、Connection 清理；
- 非 Claude、非深度 target、客户端 stream:true 不触发该聚合路径。

## 11. 实施边界

允许：

- 专用 target/options/wrapper；
- attempt reset 方法；
- 必要 DTO 字段及 presence-sensitive bool；
- 扩展现有 Claude stream state；
- Chat 专用 SSE accumulator；
- 深度 response wrapper；
- 最小错误与 header 处理辅助函数。

禁止：

- 新建通用协议转换层或 IR；
- 用模型名、渠道名或 URL 猜测 target；
- 全局改变共享 Responses/Gemini/base Claude converter 语义；
- 修复 Baidu V2/SiliconFlow 等无关路由问题；
- 改变 pass-through；
- 引入新的 cache token 扣减/计费公式；
- 为不完整上游 tool call 伪造 ID；
- 扩大模型前缀表解决 reasoning 兼容。

## 12. 验收标准

1. `/v1/messages` 经明确 Chat target 发出的 URL 与 body 符合对应 Chat Completions wire protocol。
2. 文本、图片、thinking、tool_use、tool_result、tool_choice 可完成严格客户端交互。
3. 非流式只转换 choice 0，结构错误不会伪装成功。
4. 流式 block 生命周期合法；成功流 terminal events 各一次，失败流无成功 terminal。
5. 同 chunk 多字段、多工具、乱序 arguments、nil index、identity 冲突均按本文确定规则处理。
6. `stream:false` 收到正确标注或错误标注的完整 Chat SSE 均聚合为 Anthropic JSON；错误或截断明确失败。
7. retry attempt 间 target、深度标记和 stream state 不泄漏。
8. Responses、Gemini、Ollama、native Messages、pass-through 等排除路径行为不变。
9. 所有受影响 Go package tests 通过。
10. `go build ./...` 通过。
