# 渠道优先级与权重使用说明

> 本文档基于 `model/channel_cache.go` 的 `GetRandomSatisfiedChannel` 实现整理，说明渠道选择中「优先级」与「权重」的语义与用法。

## 一句话定义

- **优先级（priority）**：负责**分层回退**，决定第几次重试落在哪一层。
- **权重（weight）**：负责**同一层内的分配比例**，是负载均衡的比例调节器。

两者是嵌套关系：先用优先级定层，再在层内按权重抽取。两者都不是开关——优先级数字决定层序，权重数字决定比例。

---

## 优先级：分层回退

### 语义

- 优先级是**整数**，**数字越大越优先**（候选优先级按倒序排序）。
- `retry`（上游重试次数）决定落到第几层：`retry=0` 取最高优先级层，`retry=1` 降一层，依此类推。
- 当 `retry >= 优先级层数` 时，钳到最低层反复重试（`channel_cache.go:158-160`）。
- 只有 `priority == targetPriority` 的渠道进入下一阶段（权重抽取）。

### 典型场景

| 配置 | 行为 |
|------|------|
| 所有渠道同优先级 | 只有一层，retry 不再跨层回退，仅在本层内重试 |
| 主渠道 priority=10、备用 priority=1 | 正常流量走主渠道；主渠道失败后 retry 增加，回退到备用层 |
| 三层优先级 10/5/1 | 依次按 retry=0→1→2 逐层降级 |

---

## 权重：层内分配

### 语义

- 权重是**非负整数**，默认 `0`（`channel.go:31`）。
- 在同一优先级层内，按权重做**加权随机**：权重大，占的随机区间大，被选中概率高（`channel_cache.go:196-218`）。
- 权重**不是负载均衡的开关**，只要同一层有 ≥2 个渠道，负载均衡就自动生效。

### 三种权重分布的处理

1. **全为 0**：触发兜底，每个渠道有效权重设为 100，变成**等概率随机**（`channel_cache.go:185-189`）。
2. **平均权重 < 10**（且不全为 0）：`smoothingFactor=100`，放大 100 倍减少取整误差（`channel_cache.go:190-193`）。
3. **混合 0 与非 0**：`sumWeight != 0` 不触发兜底，权重为 0 的渠道在加权随机中命中概率为 **0**，完全不承接流量。

> ⚠️ 关键陷阱：同一层内若**部分渠道设了权重、部分留 0**，留 0 的渠道会被静默跳过。要么全部留 0（均摊），要么全部设非 0（按比例）。

---

## 特殊路径

- **单渠道层**：某层只有一个候选时走快速路径直接返回，权重完全用不上（`channel_cache.go:131-142`）。
- **熔断器过滤**：选择前 `filterOpenCircuitBreakerChannels` 已移除 OPEN 状态的渠道，它们不参与分层也不参与加权（`channel_cache.go:119, 256-274`）。HALF_OPEN 渠道在加权循环里再单独竞争单个探测名额（`channel_cache.go:206-213`）。

---

## 使用建议

### 场景 A：纯故障回退，每层一个渠道

每层只放一个渠道，优先级设不同值（如主 10 / 备 1），权重全留 0。此时权重无意义，纯靠优先级做主备切换。

### 场景 B：单层多渠道，均摊负载

所有渠道同优先级、权重全留 0。系统自动等概率随机，每个渠道承接等量流量。

### 场景 C：单层多渠道，按比例分摊

所有渠道同优先级，按期望流量比设权重。例如 A:B = 3:1，则 A 设 30、B 设 10（或 3 与 1，平均 <10 会自动放大）。

### 场景 D：分层 + 层内负载均衡

主层多渠道同优先级、设权重比例；备用层单独一个或多个低优先级渠道。主层按比例分摊，主层整体不可用时回退到备用层。

---

## 常见误区

| 误区 | 实际 |
|------|------|
| 「不设权重就不负载均衡」 | 不设权重（全 0）= 等概率均摊，仍是负载均衡 |
| 「优先级数字越小越优先」 | 数字**越大**越优先（倒序排序） |
| 「同层留 0 权重的渠道会均摊」 | 混合场景下 0 权重渠道命中概率为 0，被跳过 |
| 「retry 会无限跨层降级」 | 超过层数后钳在最低层反复重试 |

---

## 代码位置索引

| 内容 | 位置 |
|------|------|
| 渠道选择主逻辑 | `model/channel_cache.go:109` `GetRandomSatisfiedChannel` |
| 优先级分层与 retry 落层 | `channel_cache.go:144-161` |
| 同层加权随机 | `channel_cache.go:164-218` |
| 权重兜底与平滑因子 | `channel_cache.go:181-193` |
| 单渠道快速路径 | `channel_cache.go:131-142` |
| 熔断器过滤 | `channel_cache.go:119, 256-274` |
| `Weight` / `Priority` 字段定义 | `model/channel.go:31, 45` |
| `GetWeight` / `GetPriority` | `model/channel.go:477-489` |
