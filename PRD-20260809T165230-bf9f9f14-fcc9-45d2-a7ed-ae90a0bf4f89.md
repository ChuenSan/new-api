# PRD: `/v1/messages` → `/v1/chat/completions` 深度转换

> 分支：`codex/deep-convert-messages2chat`
> 
> 范围：仅 Anthropic Messages 入站到 OpenAI Chat Completions 最终上游
> 
> 依据：当前仓库代码；参考 CC Switch 的兼容目标与思路，但不引入其架构或独立通用转换层。

## 1. 目标

在现有 Claude 请求处理、OpenAI adaptor 与 Claude 响应转换骨架中，完成严格受目标协议隔离的深度兼容：

```text
POST /v1/messages (Anthropic Messages)
  → OpenAI Chat Completions request
  → OpenAI Chat Completions response / SSE
  → Anthropic Messages response / SSE
```

使严格 Anthropic Messages 客户端可通过 OpenAI、Azure、OpenRouter Chat Completions，以及明确配置为 `anthropic_messages_to_openai_chat_completions` 的 Advanced Custom 渠道，稳定使用文本、图片、thinking、工具调用、多轮 tool_result、非流式与流式响应。

本次是在 `service.ClaudeToOpenAIRequest`、`service.ResponseOpenAI2Claude`、`service.StreamResponseOpenAI2Claude` 和 OpenAI relay handler 等已有边界上补齐规则，**不新建通用 IR 或通用协议转换框架**。

## 2. 当前代码事实与问题

### 2.1 现有链路

- `/v1/messages` 被路由为 `RelayFormatClaude`。
- `relay.ClaudeHelper` 负责模型映射、pass-through、调用渠道 adaptor、请求发送与响应处理。
- 常规 OpenAI 渠道的请求转换为：
  `openai.Adaptor.ConvertClaudeRequest` → `service.ClaudeToOpenAIRequest` → `ConvertOpenAIRequest`。
- OpenAI/Azure adaptor 当前会根据入站 `RelayFormatClaude` 将 URL 改写为 Chat Completions。
- 非流式 OpenAI 响应由 `openai.OpenaiHandler` 解析，Claude 输出时调用 `service.ResponseOpenAI2Claude`。
- 流式 OpenAI 响应由 `openai.OaiStreamHandler` 延迟一条 chunk 处理；中间 chunk 经 `HandleStreamFormat` → `StreamResponseOpenAI2Claude`，最后缓存 chunk 经 `HandleFinalResponse` 再处理。
- Advanced Custom 已有精确 converter：`anthropic_messages_to_openai_chat_completions`。

### 2.2 当前不足

当前转换是共享的浅层转换，并没有“最终目标一定为 OpenAI Chat Completions”的可靠标记。`RelayFormatClaude` 仅表示入站/下游格式，不能作为最终上游协议的判据：Messages→Responses、Gemini→OpenAI→Claude 等非目标路径也可以经过共享 converter。

现有转换还存在以下实质缺口：

- request：未映射 `tool_choice` / `parallel_tool_calls`；system array 无换行拼接；工具 schema 解析失败被吞；tool_use 与 text/image 不能共存；image URL 与空 source 未安全处理；tool_result 序列化错误被吞；历史 thinking 被丢弃。
- non-stream：未拒绝空 choices；当前拼接多 choice；DTO 不接收 refusal、legacy `function_call`；未知 stop reason 不降级；usage 不做非负保护。
- stream：converter 自行发送 terminal events；usage-only chunk 可提前终结；首 chunk 多工具只处理第一个；没有 pending tool state；不识别流内 error；EOF 无 finish reason 不保证终结。
- non-stream pseudo SSE：目前 JSON 解析失败即报错，没有聚合兜底。

## 3. 严格范围与非目标

### 3.1 唯一目标路径

仅在下列所有条件均成立时启用本 PRD 的新增深度规则：

1. 入站请求为 Anthropic Messages（`RelayFormatClaude`）；
2. 请求未使用全局或渠道级 pass-through body；
3. 最终上游目标被**显式判定**为 OpenAI **Chat Completions**；
4. 该目标判定在请求发出前已冻结，并可在 response / stream / finalizer 阶段读取。

目标路径包括：

| 渠道/配置 | 是否启用 |
|---|---|
| OpenAI Chat Completions 渠道 | 是 |
| Azure Chat Completions | 是 |
| OpenRouter Chat Completions | 是 |
| Advanced Custom route 的 converter 为 `anthropic_messages_to_openai_chat_completions` | 是 |

### 3.2 明确不做且必须维持旧行为的路径

| 路径 | 本次行为 |
|---|---|
| Messages → Responses（含 `chatCompletionsViaResponses`） | 禁用全部新增深度规则 |
| Messages → Gemini 或经 OpenAI 中转 Gemini | 禁用 |
| 原生 Anthropic Messages | 禁用 |
| DeepSeek、Moonshot 等原生 Messages 渠道 | 禁用 |
| Advanced Custom converter 为 `none` 或任意其他 converter | 禁用 |
| 任意 pass-through body | 禁用 |
| OpenAI/Gemini/Responses 入站的其他转换 | 禁用 |
| 非 Claude 下游的 `OaiStreamHandler` 调用者 | 禁用 Claude 专属新增流行为 |

不改动其他协议的请求字段语义、响应字段语义、SSE 终结语义、usage 语义或 DTO JSON 输出。

## 4. R0：目标协议隔离契约

### 4.1 单一判定与默认拒绝

在 `RelayInfo` 或与其等价的请求生命周期状态中增加一个单一、显式的布尔/枚举契约，语义为：

```text
AnthropicMessagesToOpenAIChatCompletions == true
```

它只能在“最终目标已确认是 Chat Completions”后设置。任何未设置、未知、冲突或解析失败的情况均为 `false`，并使用旧行为；不得根据模型名、厂商名、`RelayFormatClaude`、OpenAI 兼容性名称等推断为 true。

### 4.2 设置点和判定规则

- OpenAI / Azure / OpenRouter 常规 adaptor：仅当实际将请求发往 Chat Completions endpoint 时设置。
- Advanced Custom：仅当匹配到 `/v1/messages` route 且 converter **精确等于** `anthropic_messages_to_openai_chat_completions` 时设置。
- Messages→Responses 分支必须显式保持 false；不得因其内部创建了 Chat DTO 而变为 true。
- pass-through 不能设置 true。
- 目标状态必须在 adaptor 请求转换前可用，并在发送、非流式响应、流式 chunk、EOF finalization 和 pseudo-SSE fallback 中保持一致。

如果现有 endpoint/route 决议顺序不能在 `ClaudeToOpenAIRequest` 前提供该状态，可以增加最小的 options/包装入口；不得以共享函数内的渠道类型猜测替代。

### 4.3 契约使用点

下列**新增**深度行为均必须以该契约为必要条件：

- Chat 专属 request 字段、tool/schema、message 深转换；
- `ResponseOpenAI2Claude` 的本 PRD 新增 non-stream 解码规则；
- `StreamResponseOpenAI2Claude` 的本 PRD 新增状态机；
- `HandleStreamFormat` / `OaiStreamHandler` / `HandleFinalResponse` 的 Claude 专属 flush、error、terminal、pseudo-SSE 新分支。

非目标调用者必须继续走 legacy 分支。仅在上层“自觉不调用”不构成隔离；共享 converter 和 shared finalizer 内部都必须由该契约守卫。

## 5. R1：请求深度转换

本节仅在 R0 契约为 true 时生效。

### 5.1 顶层字段

| Anthropic | OpenAI Chat | 规则 |
|---|---|---|
| `model` | `model` | 使用现有模型映射后的上游模型名 |
| `max_tokens` | `max_tokens` | 基础 converter 保留；o-series/GPT-5 的 `max_completion_tokens` 规范化继续由 OpenAI adaptor 负责 |
| `temperature` / `top_p` / `top_k` | 同名兼容字段 | 延续现有 DTO 与 adaptor 处理；不新增模型前缀推断 |
| `stop_sequences` | `stop` | 1 项为 string，多项为 array |
| `stream` | `stream` | 透传 |
| `tool_choice.type=auto` | `tool_choice="auto"` | 仅在有有效 tools 时输出 |
| `tool_choice.type=any` | `tool_choice="required"` | 同上 |
| `tool_choice.type=none` | `tool_choice="none"` | 同上 |
| 指定 tool | function selector | `{"type":"function","function":{"name":...}}` |
| `tool_choice.disable_parallel_tool_use` | `parallel_tool_calls` | 仅字段显式出现时取反映射 |
| `thinking` / `output_config.effort` | reasoning 相关字段 | 仅复用已存在的 target adaptor/模型能力；普通严格 Chat 不发送非标准字段 |

`metadata`、`service_tier`、`mcp_servers`、`context_management`、`container` 等 Anthropic 专有顶层字段不得出现在 Chat request 中。

`stream_options.include_usage` 仍由目标 adaptor 的既有能力决定；本功能不得对不支持渠道强制注入。

### 5.2 system

1. string system 转单个 `role:"system"` message。
2. array system 只取非空 text block；非 OpenRouter Claude 兼容路径以 `"\n"` 连接，不能无分隔拼接。
3. OpenRouter 上游模型为 `anthropic/claude...` 时，保持已有多模态 system content 与 `cache_control` 行为。
4. system 到 developer 的模型适配仍由 OpenAI adaptor 完成，不在本 converter 重复实现。
5. 无有效 system text 时不输出空 system message。

### 5.3 tools 与 schema

1. `tools[].input_schema` 映射为 `tools[].function.parameters`。
2. tools 反序列化失败必须返回转换错误；不可忽略错误后发送空工具数组。
3. 每个 tool 必须有非空 `name`；schema 必须可表示为 JSON object，否则返回转换错误。
4. 仅递归删除值严格等于 `"uri"` 的 `format` 字段；递归至少覆盖 `properties`、`items`、嵌套 object 和 array。
5. 保留 `$schema`、`$defs`、`additionalProperties`、`patternProperties` 等其他 schema 关键字；不得做激进 schema 清洗。
6. 若转换后没有有效 tools，则不得输出 tool_choice / parallel_tool_calls，避免悬空强制工具选择。

### 5.4 messages、图片与工具回传

1. string content 原样转换。
2. content array 的 text、image、thinking、tool_use 可以同一 assistant message 共存。
3. `tool_use` 映射到 assistant `tool_calls[]`：
   - 保留 `id`；
   - `name` → `function.name`；
   - `input` 用稳定 JSON 序列化为 `function.arguments`；序列化失败返回错误。
4. assistant 同时含 text/image 与 tool_use 时，保留 content 与 `tool_calls`；允许 Chat assistant 的 `content` 为 null 仅限确实没有 text/image 时。
5. `tool_result` 必须拆为独立 `role:"tool"` message：
   - `tool_call_id=tool_use_id`；
   - string content 原样写入；
   - array/object content 序列化为 JSON string；失败返回错误；
   - 不依赖 tool name 才能建立关联。
6. image：
   - `source.type=base64` → `data:{media_type};base64,{data}`；
   - `source.type=url` → 原 URL；
   - source 为 nil、media type/data/URL 为空、未知 source type 均返回明确转换错误，不得 panic。
7. 历史 thinking：
   - 仅在明确 target adaptor 已支持的字段下发送 `reasoning_content`；
   - 普通严格 Chat 路径不得发送非标准 reasoning 字段；
   - `redacted_thinking` 使用固定、可测试的降级值，或按该 target 的明确兼容规则丢弃；选择必须由测试锁定。
8. 最终没有 content 和 tool_calls 的普通 message 不发送；含 tool_calls 的 assistant message 可以 `content:null`。

## 6. R2：非流式 Chat 响应 → Anthropic 响应

仅在 R0 契约为 true 时使用本节新增解码/校验规则。

### 6.1 结构校验与 choice

1. `OpenaiHandler` 完成 JSON 解码后，若上游 error 已存在，仍按既有上游错误处理。
2. `choices` 必须非空；空 choices 返回 bad response，不能生成空成功 Claude response。
3. 本功能只使用 `choices[0]`；不得把多个 choice 拼接为一个 Anthropic message。

### 6.2 content block 生成

输出 block 的顺序固定为：

1. thinking；
2. text / refusal；
3. tool_use。

规则：

- `message.reasoning_content` 或 `message.reasoning` → `{type:"thinking", thinking:...}`。
- `message.content` 是 string → text block。
- content array 中 `text` / `output_text` → text block；part-level `refusal` → text block。
- message-level `refusal` → text block。
- DTO 必须显式支持上述 `refusal` 和 legacy `function_call` 字段，以保证 JSON 不被丢弃。
- `tool_calls[]` → 一个或多个 `tool_use` block。
- legacy `function_call` 与 `finish_reason:"function_call"` 必须成套支持，映射为单个 tool_use；不能仅映射 stop reason。
- tool arguments 为合法 JSON object 时用作 `input`；为空、非 object 或非法 JSON 时使用 `{}` 并记录 warn。

### 6.3 stop reason 与 usage

finish reason 映射：

| 上游值 | Anthropic `stop_reason` |
|---|---|
| `stop` | `end_turn` |
| `length` / `max_tokens` | `max_tokens` |
| `tool_calls` / `function_call` | `tool_use` |
| `content_filter` | 延续项目既有 refusal 处理 |
| 未知 | `end_turn`，记录 warn |

usage：

- `prompt_tokens` → `input_tokens`，`completion_tokens` → `output_tokens`。
- cache read / cache creation 不能与 `input_tokens` 重复相加。
- 上游 usage 缺失时仍返回非 nil 的零值 Claude usage。
- 所有 token 数在输出前钳制为非负数。

## 7. R3：流式 Chat SSE → Anthropic SSE

仅在 R0 契约为 true 时采用本节状态机。非目标路径维持原 stream 行为。

### 7.1 状态与职责边界

将本功能新增的 Claude stream 状态置于 `RelayInfo` 的 request 生命周期内。它至少需要表达：

- 当前已打开的 text/thinking/tool block；
- 稠密的 Anthropic content index；
- upstream tool index → Anthropic index 映射；
- 每个 pending tool 的 upstream index、ID、name、pending arguments、是否已 start；
- 首个非空 finish reason；
- 最后一个非 nil upstream usage；
- 是否已发送 error；
- 是否已发送 success terminal。

`StreamResponseOpenAI2Claude` 仅生成 `message_start` 与 content-block 事件，不得生成 `message_delta` 或 `message_stop`。成功 terminal 由一个契约受保护的 finalizer 单点发送。

### 7.2 chunk、flush 与 EOF

1. 保持当前延迟一条 chunk 的读取模型，但 scanner 结束后必须将尚未交给 converter 的最后一个有效 data chunk 交给本目标状态机处理。
2. usage-only chunk 只更新“最后一个 upstream usage”，不能关闭 message 或发送 terminal。
3. `finish_reason` 采用 first-wins：只记录第一个非空值，后续不覆盖。
4. 正常 `[DONE]` 或 EOF：
   - 幂等关闭所有已启动但未关闭的 content block；
   - 若无 finish reason，以 `end_turn` 终结；
   - 发送且只发送一次 `message_delta` 与 `message_stop`。
5. finalizer 必须能在 `lastStreamData` 为空、最后 chunk 只有 usage、或最后 chunk 不是可转换 choice 时从缓存状态完成终结；不能依赖“再解析最后一条 JSON”才可终结。
6. block 已关闭时 finalizer 不得重复发送 `content_block_stop`。

### 7.3 thinking、text 与工具

1. thinking 和 text 切换前，先停止旧 block，再开始新 block。
2. 一个 delta 内的全部 `tool_calls[]` 都要处理。
3. Anthropic content index 必须按首次可发送 block 的到达顺序稠密分配，不能直接复用 upstream index。
4. 如果 arguments 先到而 ID/name 尚不完整，缓存 arguments，不发送不完整的 start。
5. tool 的 ID 与 name 均可用后，发送 `content_block_start`，随后按到达顺序 flush 已缓存 arguments 和后续 arguments。
6. stream 结束时：
   - ID 缺失时按 `upstream tool index` 生成固定、稳定、可测试的 placeholder ID；
   - name 缺失的 tool 视为无效，丢弃并记录 warn；
   - 已完整的 pending tool 必须补发 start 后再关闭。
7. 首 chunk 含多个完整 tools 时，必须为全部 tools 生成 start/delta，不能只处理第一个。

### 7.4 流内 error

1. 每个普通 `data:` payload 在作为 Chat chunk 解码前都必须识别 OpenAI error envelope（至少 `{"error": ...}`）。
2. 识别到 error 后，编码一个符合 Anthropic `event:error` 的错误事件。
3. error 只能发送一次；停止继续读取/转换；不得再发送成功 `message_delta` 或 `message_stop`。
4. 错误检测、scanner callback 和 finalizer 必须共享同一 error 状态，避免 error 后 finalizer 补成功终结。

### 7.5 UTF-8

保持 `StreamScannerHandler` 的行扫描架构。新增测试将多字节 UTF-8 字符拆在底层 read 边界；若测试证明 `bufio.Scanner` 当前行为正确，则不引入额外字节缓冲层。

## 8. R4：非流式伪 SSE 兜底

仅当 R0 契约为 true，且调用者请求非流式时启用。

1. 位置为 `openai.OpenaiHandler`：读取完整 body 后首次 JSON 解码失败时尝试 SSE 嗅探。
2. 不得将此逻辑放入只在 `info.IsStream` 下执行的流式 handler。
3. SSE 嗅探成功后聚合为一个 `OpenAITextResponse`，再调用 `ResponseOpenAI2Claude`：
   - content 与 reasoning 增量按顺序拼接；
   - tool_calls 按 upstream index 聚合 ID/name/arguments；
   - finish reason first-wins；
   - usage 取最后一个非 nil 值。
4. 任一 SSE error envelope 必须返回上游错误，不得聚合成成功 Anthropic message。
5. 无有效 choice，或既没有 finish reason 也没有 `[DONE]` / 明确完成标识的结果，按截断/坏响应返回错误。
6. 非目标协议、普通 JSON、OpenAI 下游、Gemini 下游及 pass-through 均不尝试本 fallback。

## 9. 实施边界

优先在以下已有文件/边界中实现最小改动：

- `relay/claude_handler.go`：入站、Responses 排除与目标契约初始化。
- `relay/common/relay_info.go`（或同级生命周期状态）：目标契约及 Claude 深转换流状态。
- `relay/channel/openai/adaptor.go`：常规 Chat endpoint 的目标契约设置、request 适配复用。
- `relay/channel/advancedcustom/adaptor.go`：精确 converter 的目标契约设置与保留传递。
- `service/convert.go`：受契约保护的 request/non-stream/stream 深转换。
- `relay/channel/openai/relay-openai.go`、`relay/channel/openai/helper.go`：伪 SSE、stream flush、错误与单点 finalizer。
- `dto/openai_request.go`、`dto/openai_response.go`：仅为接收和表达本 PRD 必须的 OpenAI Chat 字段做最小 DTO 扩展。

不得把新增规则无条件塞进共享 converter，进而改变 Gemini、Responses、原生 Messages 或其他 `OaiStreamHandler` 调用者。

## 10. 测试与验收

### 10.1 协议隔离（必测）

- 常规 OpenAI、Azure、OpenRouter Chat Completions 的目标契约为 true。
- Advanced Custom 仅精确 converter `anthropic_messages_to_openai_chat_completions` 为 true。
- Messages→Responses、Gemini 中间转换、原生 Anthropic、DeepSeek/Moonshot 原生 Messages、Advanced Custom `none`、未知 converter、pass-through 均为 false。
- 目标标记缺失/未知/冲突时 fail-closed，走旧行为。
- 对 Responses bridge、Gemini→OpenAI→Claude 的 non-stream、stream chunk 与 finalizer，断言不启用本功能新分支。
- 对其他 `OaiStreamHandler` 调用者，断言 Claude 专属新 terminal/error 行为不泄漏。

### 10.2 请求单测

- system string、array、多 block、空 block、换行连接。
- `tool_choice` 四种类型与 `disable_parallel_tool_use`。
- 无有效 tools 时移除悬空 tool_choice。
- schema 的递归 `format:"uri"` 清理和其他关键字保留。
- 非法 tools、缺 name、非法 schema 返回错误。
- text + image + tool_use 共存。
- base64 image、URL image、nil source、空值、未知 source。
- 多轮 tool_use + tool_result；tool_result JSON 序列化失败。
- thinking / redacted_thinking 的目标与非目标行为。
- o-series/GPT-5 max token 正规化继续由 adaptor 生效。

### 10.3 非流式响应单测

- 空 choices 为 bad response。
- 仅 choices[0] 生效。
- thinking + text + 多 tool_calls 的 block 顺序。
- message-level 与 content-part refusal。
- legacy function_call。
- 全部 finish reason 映射与未知值 warn 降级。
- 合法、空、非法、非 object tool arguments。
- cache usage 不重复计算、缺 usage 返回零值 usage、负 token 钳制。

### 10.4 流式单测

- 纯文本完整事件序列与成功 terminal 单次。
- thinking → text、text → tool 的 block 生命周期。
- 多 tool calls、首 chunk 多 tool calls、非连续 upstream index 的稠密映射。
- arguments 早于 ID/name；结束时 placeholder ID；缺 name 丢弃。
- 多 finish reason first-wins。
- finish 后 usage-only chunk；usage-only 不提前终结。
- 无 finish reason 的 `[DONE]` 与 EOF。
- 最后缓存 chunk flush、空 last data finalization。
- 上游 error 仅一个 Anthropic error，error 后无 success terminal。
- UTF-8 多字节字符跨底层 read 边界。

### 10.5 非流式伪 SSE 单测

- `stream:false` 但上游 body 为完整 SSE：text、reasoning、tool calls、usage 聚合。
- SSE error 返回上游错误。
- 无 choice、无 completion 标志、截断 SSE 返回错误。
- 非目标路径 JSON 解码失败不触发伪 SSE 聚合。

### 10.6 最终验收

1. Anthropic 客户端经 `/v1/messages` 调 OpenAI Chat Completions，可完成文字、图片、thinking、工具调用和 tool_result 多轮。
2. 非流式输出符合 Anthropic Messages 结构，流式输出满足 content-block 生命周期与 terminal 单次语义。
3. 上游 SSE error 永不包装为成功 Anthropic message。
4. 非流式伪 SSE 能可靠聚合或明确失败。
5. 所有列明非目标路径的行为由针对性回归测试证明未启用本功能。
6. `go build ./...` 通过，受影响包测试通过。
