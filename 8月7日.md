# New API 协议转换缺陷:Claude Code 报「Content block not found」根因分析与修复方案

> 2026-08-07 · 状态:根因已定位(线上实锤),待修复
> 适用代码:本仓库 `service/convert.go`(`StreamResponseOpenAI2Claude`)

---

## 1. 现象

- Claude Code 客户端(v2.1.220)在流式会话中途报错:`API Error: Content block not found`,流中断。
- 链路上的所有 New API(中转入口 + 上游)**日志均无报错**:请求返回 200,`stream_status: ok`,`end_reason: done`,正常计费。
- 错误是 **Claude Code 客户端在本地解析 SSE 流时抛出的**,不会回传服务端,因此服务端日志永远看不到。

## 2. 触发链路

```
Claude Code ──POST /v1/messages(Anthropic Messages 格式)──► New API
    │  New API 按渠道配置做协议转换:
    │    请求:ClaudeToOpenAIRequest(service/convert.go:17)
    │    响应:StreamResponseOpenAI2Claude(service/convert.go:255)
    ▼
上游 OpenAI 兼容服务(本例:DeepSeek-V4-Flash,/v1/chat/completions,SSE)
```

只有「**Claude 格式入站 + OpenAI 兼容上游 + 流式 + 响应同时包含 reasoning(思考)和 tool_calls(工具调用)**」四个条件同时满足时才会触发。

## 3. 根因

### 3.1 客户端侧(错误字串来源)

Claude Code 2.1.220 二进制内置错误码 `content_block_not_found_delta`。客户端流式状态机用数组 `cr[index]` 跟踪已开启的 content block;收到 `content_block_delta` 事件时,若 `cr[delta.index]` 不存在(该 index 从未收到过 `content_block_start`),直接抛:

```
RangeError("Content block not found")
```

Anthropic SSE 协议要求 content block 的 index 从 0 开始**连续递增**。

### 3.2 上游实际发送的数据(线上抓取,124 个 SSE chunk)

```
chunk   0-94 : reasoning_content(95 个思考 delta)
chunk  95-120: tool_calls,所有 chunk 的 index 均为 1   ← 关键点
chunk 121    : finish_reason = "tool_calls"
chunk 122    : usage-only chunk
chunk 123    : [DONE]
```

上游把 reasoning 占用了自己 tool_calls 数组的 index 0 槽位,**tool_call 的 OpenAI 侧 index 从 1 开始(而非 0)**。DeepSeek/Kimi 一类上游有此行为。

### 3.3 New API 转换层的缺陷(`service/convert.go`)

`StreamResponseOpenAI2Claude` 把 OpenAI tool_calls 的原始 index **直接叠加**到 Claude content block index 上:

```go
// convert.go:501 附近
blockIndex := base + offset   // base = ClaudeConvertInfo.ToolCallBaseIndex
                              // offset = OpenAI tool_calls[].index(原始值)
```

本次事故的实际推演:

| 步骤 | Claude 侧事件 | Claude index |
|---|---|---|
| 95 个 reasoning delta | thinking 块 start/delta/stop | 0 |
| thinking 结束后 `stopOpenBlocksAndAdvance` | Index 前进到 1,`ToolCallBaseIndex = 1` | — |
| tool_call(OpenAI index=1) | `blockIndex = 1(base) + 1(offset) = 2` | **2** |
| 收尾 `stopOpenBlocks`(convert.go:268) | 只对 `base+offset = 2` 发 stop | 2 |

最终发给客户端的 index 序列是 **0(thinking)、2(tool_use)——index 1 从未出现**。客户端收到 index=2 的 `content_block_start`/`content_block_delta` 时,内部数组只有 index 0,`cr[2]` 为 undefined → 抛出 `Content block not found`,流被客户端中止。

### 3.4 一句话结论

**New API 流式转换把「OpenAI tool_calls 的数组下标」当作「Claude content block 的序号」直接混用;当上游的 tool_calls index 不从 0 开始(如 reasoning 占了 0 号槽)时,产出的 Claude SSE index 序列出现空洞,客户端按连续 index 假设解析即崩溃。**

## 4. 修复方案

核心:**Claude content block index 与 OpenAI tool_calls index 解耦**,前者必须保证从当前空闲位开始连续分配。

在 `StreamResponseOpenAI2Claude` 的状态(`relaycommon.RelayInfo.ClaudeConvertInfo`)中:

1. 增加映射 `map[openaiToolIndex]claudeBlockIndex`:某个 OpenAI tool index **首次出现**时,按顺序分配下一个连续的 Claude block index 并记录;后续 chunk 查表复用。
2. `content_block_start`(tool_use)只在映射新建时发一次;`input_json_delta` 始终发往映射后的 index。
3. 关块(`stopOpenBlocks` / 收尾)遍历「实际开过的块」逐个发 `content_block_stop`,不要用 `base+offset` 区间推算。
4. 注意 `SendResponseCount == 1` 的首 chunk 特判分支(convert.go:318-355 硬编码 `SetIndex(0)`)要走同一套分配逻辑。

参照实现:CC Switch `streaming.rs` 的 `tool_blocks_by_index: HashMap<usize, ToolBlockState>`——按 OpenAI index 建独立状态,再映射到稳定的 Anthropic index;另含乱序缓冲(`pending_args`/`late_tool_starts`,处理 arguments 先于 id/name 到达)可一并移植。

## 5. 验证方法

1. 用本缺陷的真实流量做回归:上游 chunk 序列 = 95×reasoning + tool_calls(index 恒为 1)+ finish_reason=tool_calls(已存档,可向维护者索取)。
2. 断言转换输出的 Claude SSE:所有 content block 的 index 序列连续(0,1,2,...),每个 start 都有配对 stop,`message_delta` 带完整 usage 后接 `message_stop`。
3. 补充用例:tool_calls index 乱序到达(arguments 先于 name)、多并行 tool(index 0 和 1 交错)、首 chunk 即带 tool_calls。

## 6. 排查备忘(以后遇到「客户端报错、服务端无日志」时)

- 「Content block not found」是**客户端本地错误**,先查客户端会话记录(`~/.claude/projects/<项目>/<session>.jsonl`,搜错误字串,拿精确 UTC 时间)。
- 按时间戳在 New API 容器日志找请求 ID(`grep "POST /v1/messages"`),再按请求 ID 拉全链路 DEBUG 日志(`stream scanner data` 行即上游原始 SSE chunk)。
- 将 chunk 按到达顺序还原(提取 `tool_calls[].index`、`finish_reason`、reasoning/content 分布),对照 §3.2 即可确认。
