# new-api 高级设置解析

> 更新于 2026-07-09T17:30  
> 补充辨析（易错点 / User-Agent / 模型条件）见：`2026-07-15T渠道参数覆盖与请求头覆盖辨析.md`

渠道编辑页面的高级设置（Advanced Settings）包含两个独立的配置区域，分别作用于**请求 HTTP Header**与**请求 JSON Body**，互不干扰。

---

## 1. Header 覆盖（Header Override）

配置进入数据库 `channels.header_override` 字段，在**发送给上游的 HTTP 请求头**层面生效。

### 1.1 支持三种规则

| 规则类型 | 语法 | 含义 |
|---|---|---|
| **全透传** | `"*": true` | 将客户端请求的所有 header（跳过敏感 header，见下方列表）透传至上游 |
| **正则透传** | `"re:^X-Trace-.*$": true` 或 `"regex:^X-Custom-.*$": true` | 将**名称匹配 Go regexp**的客户端 header 透传至上游 |
| **强制覆盖** | `"User-Agent": "claude-cli/2.1.199 (external, cli)"` | 无论客户端是否携带该 header，均设置为此值 |

Header 名称在匹配时统一转为**小写**处理。

### 1.2 处理顺序

passthrough（透传）先执行 → explicit override（显式覆盖）后执行。因此显式覆盖的 header 会覆盖掉透传过来的同名 header。

### 1.3 支持的占位符

| 占位符 | 语法 | 说明 |
|---|---|---|
| API Key 替换 | `{api_key}` | 替换为当前渠道的 API Key |
| 客户端 header 透传 | `{client_header:<header名>}` | 从客户端请求 header 中取值，**整个 value 必须是该占位符**。客户端没带对应 header 时**跳过不设置** |

⚠️ **限制**：`{client_header:User-Agent}` 不支持 fallback 语法（如 `{client_header:User-Agent|default_value}`），客户端没带就跳过。如需 fallback 效果，直接写死强制覆盖值即可。

### 1.4 透传时跳过的敏感 header

以下 header 即使启用 `"*"` 全透传，也**不会被**转发到上游：

```
connection, keep-alive, proxy-authenticate, proxy-authorization,
te, trailer, transfer-encoding, upgrade,
cookie, host, content-length, accept-encoding,
authorization, x-api-key, x-goog-api-key,
sec-websocket-key, sec-websocket-version, sec-websocket-extensions
```

### 1.5 配置示例

**仅透传客户端 header，不强制覆盖：**
```json
{
  "*": true
}
```

**透传客户端 header + 强制覆盖认证：**
```json
{
  "*": true,
  "Authorization": "Bearer {api_key}"
}
```

**透传客户端 header + 强制覆盖 User-Agent 和认证：**
```json
{
  "*": true,
  "User-Agent": "claude-cli/2.1.199 (external, cli)",
  "Authorization": "Bearer {api_key}"
}
```

**Claude CLI 请求头覆盖：**
```json
{
  "*": true,
  "Authorization": "Bearer {api_key}",
  "User-Agent": "claude-cli/2.1.199 (external, cli)"
}
```

**仅透传特定前缀 header，其余强制覆盖：**
```json
{
  "re:^X-Stainless-.*$": true,
  "User-Agent": "claude-cli/2.1.199 (external, cli)",
  "Authorization": "Bearer {api_key}"
}
```

---

## 2. 参数覆盖（Param Override）

配置进入数据库 `channels.param_override` 字段，在**发给上游的 JSON Body** 层面生效。

支持两种格式：旧版 flat key-value（兼容）和 新版 operations 数组（推荐）。

### 2.1 旧版格式（legacy flat）

直接声明 JSON 顶层字段 key → value，做**字面 set**：

```json
{
  "model": "claude-opus-4-8",
  "temperature": 0.5
}
```

效果：无论客户端传了什么 `model` 和 `temperature`，发给上游时都被强制覆盖为上述值。

### 2.2 新版格式（operations 数组）

通过 `"operations"` 数组声明一组有序操作，每个操作包含：

| 字段 | 说明 |
|---|---|
| `mode` | 操作类型（必填），见下方 mode 列表 |
| `path` | 操作的 JSON 路径，支持 `a.b.c` 嵌套路径和 `*` 通配符 |
| `value` | 操作值，含义随 mode 不同 |
| `keep_origin` | `true` 时保留原始值（mode 不同含义不同） |
| `from` / `to` | 用于 move/copy/replace/sync_fields 等 mode |
| `conditions` | 条件过滤，满足条件才执行本操作 |
| `logic` | 条件逻辑：`"AND"`（默认）或 `"OR"` |

操作按数组顺序依次执行，前一个操作的结果作为后一个的输入。

#### 2.2.1 JSON Body 操作 mode

| mode | 说明 | 示例 |
|---|---|---|
| `set` | 设置字段值。`keep_origin: true` 时若字段已存在则跳过 | `{"mode":"set","path":"model","value":"claude-opus-4-8"}` |
| `delete` | 删除字段 | `{"mode":"delete","path":"top_p"}` |
| `move` | 移动字段值（源删除，目标写入） | `{"mode":"move","from":"old_field","to":"new_field"}` |
| `copy` | 复制字段值（源保留） | `{"mode":"copy","from":"temperature","to":"backup_temp"}` |
| `prepend` | 字符串/数组前追加 | `{"mode":"prepend","path":"model","value":"prefix-"}` |
| `append` | 字符串/数组后追加 | `{"mode":"append","path":"model","value":"-suffix"}` |
| `trim_prefix` | 去掉字符串前缀 | `{"mode":"trim_prefix","path":"model","value":"prefix-"}` |
| `trim_suffix` | 去掉字符串后缀 | `{"mode":"trim_suffix","path":"model","value":"-suffix"}` |
| `ensure_prefix` | 确保字符串有前缀（没有则加上） | `{"mode":"ensure_prefix","path":"model","value":"prefix-"}` |
| `ensure_suffix` | 确保字符串有后缀 | `同上` |
| `trim_space` | 去掉字符串首尾空白 | `{"mode":"trim_space","path":"model"}` |
| `to_lower` | 字符串转小写 | `{"mode":"to_lower","path":"model"}` |
| `to_upper` | 字符串转大写 | `{"mode":"to_upper","path":"model"}` |
| `replace` | 字符串字面替换 | `{"mode":"replace","path":"model","from":"-old","to":"-new"}` |
| `regex_replace` | 字符串正则替换（Go regexp） | `{"mode":"regex_replace","path":"model","from":"-(old|beta)","to":"-stable"}` |
| `return_error` | 拒绝请求，返回指定错误 | 见下方示例 |
| `prune_objects` | 按条件**删除 JSON 对象/字段** | 见 2.3 节详细说明 |

**Body 参数覆盖示例（改 temperature，不是改 HTTP 头）：**
```json
{
  "operations": [
    {
      "mode": "set",
      "path": "temperature",
      "value": 0.7
    }
  ]
}
```

⚠️ **易错：** `mode: "set"` + `path: "User-Agent"` 改的是 **JSON Body 字段**，不是 HTTP `User-Agent` 请求头。固定上游 UA 请用 **请求头覆盖**，或参数覆盖里的 `set_header`。详见 `2026-07-15T渠道参数覆盖与请求头覆盖辨析.md`。

#### 2.2.2 HTTP Header 操作 mode

以下 mode 操作的是**发给上游的 HTTP Header**，而非 JSON Body（经内部 `header_override` 上下文 → `RuntimeHeadersOverride`）：

| mode | 说明 |
|---|---|
| `set_header` | 设置 header 值。`keep_origin: true` 时若目标 header 已有值可跳过 |
| `delete_header` | 删除 header |
| `copy_header` | 复制 header（源保留） |
| `move_header` | 移动 header（源删除） |
| `pass_headers` | 将**客户端 HTTP 请求头**（上下文 `request_headers`）按名拷到运行时 `header_override` |

#### 2.2.3 `pass_headers` 详解

`pass_headers` 实现为 `copyHeaderInContext`：源是客户端 **HTTP Header**（`request_headers`），不是 Body 里的同名字段。

**处理模型简述：**

1. 客户端 Body 上执行 set/delete 等 body 操作  
2. 客户端 HTTP 头注入上下文 `request_headers`；渠道 Header Override 注入 `header_override`  
3. `pass_headers`：按名从 `request_headers` 拷到 `header_override`（客户端没带则跳过）  
4. 结束后同步为 `RuntimeHeadersOverride`，发送时 `processHeaderOverride` 使用  

**`keep_origin`：** 目标 header 在内部 context 已有值时，`true` 则保留不覆盖。

**固定 UA vs 透传 UA：**

- 固定成某一字符串 → **请求头覆盖** 写死 `User-Agent`，或参数覆盖 `set_header`  
- 原样透传客户端 UA → `pass_headers` 含 `User-Agent`，或请求头覆盖 `"User-Agent": "{client_header:User-Agent}"`

**示例（透传客户端若干头）：**
```json
{
  "operations": [
    {
      "mode": "pass_headers",
      "value": ["User-Agent", "X-Stainless-Arch"],
      "keep_origin": true
    }
  ]
}
```

value 支持：字符串数组 `["header1","header2"]`、逗号分隔字符串、或对象 `{"headers":["header1"]}`。

#### 2.2.4 `return_error` 示例

```json
{
  "mode": "return_error",
  "value": {
    "message": "This feature is not supported on this channel",
    "code": "unsupported_feature",
    "type": "invalid_request_error",
    "status_code": 400,
    "skip_retry": true
  }
}
```

#### 2.2.5 条件过滤

每个操作可加 `conditions` 数组，满足条件才执行：

```json
{
  "mode": "set",
  "path": "model",
  "value": "fast-model",
  "conditions": [
    {
      "path": "messages.#",
      "mode": "gte",
      "value": 50
    }
  ]
}
```

条件 mode 支持：`full`（完全匹配）、`prefix`、`suffix`、`contains`、`gt`、`gte`、`lt`、`lte`。`invert: true` 取反，`pass_missing_key: true` 控制字段缺失时的行为。

### 2.3 `prune_objects` 详解

`prune_objects` 根据条件递归删除 JSON 对象中的**不匹配字段/元素**。它用于清理 body 中多余的、不在白名单内的字段。

**最简单的用法：传入字符串表示按 `type` 字段过滤。** 但通常用对象格式灵活配置。

**示例——删除 root 下所有不匹配条件的字段：**
```json
{
  "mode": "prune_objects",
  "path": "",
  "value": {
    "logic": "OR",
    "recursive": true,
    "where": {
      "key": "model|messages|temperature|top_p"
    }
  }
}
```

客户端发来：
```json
{
  "model": "claude-sonnet-5",
  "messages": [...],
  "temperature": 0.7,
  "top_p": 0.9,
  "custom_field": "xxx",
  "frequency_penalty": 0.5
}
```

`prune_objects` 删掉不在 `where.key` 名单里的字段，发给上游：
```json
{
  "model": "claude-sonnet-5",
  "messages": [...],
  "temperature": 0.7,
  "top_p": 0.9
}
```

`custom_field` 和 `frequency_penalty` 被移除。

**与 `pass_headers`：** 二者无关。`prune_objects` 只裁 body；`pass_headers` 只从客户端 HTTP 头拷到运行时 header override。没有 `prune_objects` 时 body 字段默认原样保留。

---

## 3. Header Override 与 Param Override 的关系

- **Header Override**：静态配置，由 `processHeaderOverride` 读入后写到上游 `http.Request.Header`
- **Param Override**：主体改 JSON Body；其中的 `set_header` / `pass_headers` 等会写入 `RuntimeHeadersOverride`，并设 `UseRuntimeHeadersOverride=true`

`GetEffectiveHeaderOverride`：**一旦启用运行时覆盖，就以运行时结果为准**（运行时 map 的初始内容来自渠道 Header Override，再被 param operations 修改）。不是「两边简单后写覆盖前写」的独立双轨。

**选型：**

- 固定改头 → 优先 **Header Override**
- 改 body / 条件逻辑 / 复杂 header 操作 → **Param Override**
- 不要用 `set` + header 名假装改 HTTP 头

更完整的易错辨析见：`2026-07-15T渠道参数覆盖与请求头覆盖辨析.md`。
